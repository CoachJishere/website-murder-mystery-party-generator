# ADR-0103: New-Purchase Coherence Sweep — a standing practice, not a one-off audit

- **Status:** Accepted
- **Date:** 2026-08-22
- **Related:** ADR-0098 Addenda 4-8 (the audit this ritual is distilled from), ADR-0074 (blanket Sonnet 5 upgrade), ADR-0042 (the original content-quality detectors this ritual leans on and works around)

## Context

A single hands-on audit of one customer's package (Lyn DiFranco, ADR-0098 Addendum 4) turned up real, customer-facing coherence bugs — a victim-name mismatch, a self-contradicting secret, dangling references to removed characters, a garbled sentence, a leaked prompt placeholder — none of which the existing automated content-quality detectors (ADR-0042) caught. Widening the sweep found the same defect family in 14 more packages across a year of history (Addenda 5-6), and confirmed the detectors themselves had real bugs of their own (a `\b`-vs-`\M` regex mistake that silently disabled a whole marker family, and a false positive on a legitimate template token) — bugs only found by re-verifying results by hand rather than trusting a clean detector run at face value.

Jonathan now gets a paid purchase roughly every day or few days and wants to spot-check each one for coherence, the way today's audit did, without either (a) re-deriving the whole methodology from a blank prompt each time, or (b) the checklist quietly drifting/forgetting steps between sessions. He asked, in effect, for the same kind of low-friction ritual this project already has for session-closing and inbox-processing — paste a purchase notification into a fresh tab, say one word, get the same thorough check every time.

## Decision

Adopt **"sweep"** as a project shorthand command (documented in `CLAUDE.md`, which every fresh session auto-loads — no separate memory/pre-prompt setup needed). Invoking it with a pasted purchase notification (conversation ID is the only required field) runs the following checklist:

1. **Resolve the package.** From `conversation_id`: `package_id`, `created_at`, `mystery_style`, `player_count`, `has_accomplice`, `is_paid`, `purchase_date`, and the conversation's language/theme.
2. **Note the model era.** Pre- or post- 2026-08-11 (ADR-0074's blanket Sonnet 5 upgrade). Pre-upgrade packages get extra scrutiny — that's where every defect found so far has clustered — but post-upgrade is not skipped; the one post-upgrade package checked so far needed a full manual read anyway because it was non-English (see step 6).
3. **Run the two patched detectors** scoped to this package (`list_packages_with_meta_text_leak`, `list_packages_with_victim_mismatch`). Treat a clean result as a data point, not proof — both have documented blind spots (ADR-0098 Addendum 6): `victim_mismatch`'s core check still cannot catch a victim surname that's wrong but legitimately belongs to a different, living character in the same package, which was Lyn's actual bug.
4. **Manual victim-name consistency check.** Extract the victim's *exact* name string from `game_overview` and `detective_script`; cross-check against what 3-4 characters' own background/relationships sections call the victim, plus `evidence_cards`. Check for exact-string agreement, not just name *presence* — dual-gender placeholders like `"Margot/Marc Dubois"` are the intentional default convention for every named person in this generator (including the victim; the host is expected to resolve suspects manually via the `character_name` field, see Addendum 3), so `"does the character text contain the victim's name"` will pass even when one document says `"Margot"` and another still says `"Margot/Marc"` — a real, high-severity mismatch a substring-presence check cannot see. Compare the literal strings, not just whether one contains the other.
5. **Read the full detective_script** (or a large sample) end to end for leaked brackets/placeholders, garbled sentences (another character's name inserted mid-sentence), and — if `mystery_style = 'character'` (slip-style) — confirm any `[MURDERER NAME]`-style bracket is the *legitimate* host-fill-in convention (paired with an explicit stage direction telling the host to write in the name), not an accidental leak. Bracket patterns are not inherently bugs; context decides.
6. **Spot-check 2-3 characters' full content** (relationships, secrets, round scripts, accusations) for leaked authoring notes, self-directed questions, dangling references to removed characters (relevant only if this package went through Remove-a-Character), and broken/garbled sentences. **If the package is non-English, do this check in that language** — the automated detectors are English-pattern regexes and will not catch an equivalent leak in Spanish, French, etc. (confirmed clean on the one non-English package checked so far, but only because it was checked by hand, not because the detectors would have caught a problem).
7. **Full-schema field-completeness check, from the actual column list, not a remembered subset.** Pull `information_schema.columns` for `mystery_characters` directly rather than checking whichever fields seem relevant from memory — a hand-picked subset is exactly how a real gap (Grant/Gracie Whitfield's `accusations`, then `rumors`, both missed on the first two passes of this exact package — see Addendum 6) slipped through a "manual completeness check" that felt thorough at the time. Check two distinct shapes, both required:
   - **Partial nulls** (some characters in the package have the field, others don't). This shape is now caught automatically by `validate_package_characters()` for any package completing on or after 2026-08-30 (ADR-0113) — still worth a quick confirm on older packages, but no longer needs manual vigilance for new purchases.
   - **Uniform nulls** (every character in the package is missing the same field). ADR-0113's trigger cannot see this shape — its check only compares characters against each other within a package, so a field that's wrong for 100% of characters looks "consistent" to it. This is not hypothetical: checking for it directly found 12 whole packages with `secret` null on every character, invisible to the partial-null method (ADR-0113 Addendum, 2026-08-30). Run this check regardless of when the package completed.
   - **When two similarly-named columns exist for what looks like the same concept** (e.g. `secret` vs `secrets`), don't assume either is authoritative — grep the actual frontend render paths (`src/components/*.tsx`, `src/pages/*.tsx`, `src/services/*.ts`) for which one customers actually see before treating a populated "twin" field as evidence the content made it to the customer. `secrets` (plural) turned out to feed a component (`CharacterDetailView.tsx`) that isn't imported anywhere in the live app — real content sitting there was invisible to every customer despite passing an `IS NOT NULL` check.
8. **Run `package_completion_blocking_defects()`** to catch genuinely missing content — a different defect class (empty fields) from the leaked/garbled-text family everything else above targets.
9. **If something is found:** fix directly (targeted SQL edits, matching the patterns already established), re-verify with the same checks, and log a CHANGELOG entry plus an addendum to *this* ADR (not ADR-0098 — that ADR's own topic is a specific stuck-invocation incident already carrying 8 addenda on an adjacent-but-distinct investigation; this one is the dedicated home for the sweep ritual and its findings going forward).
10. **If the package is clean:** report back in chat only. No CHANGELOG entry, no ADR addendum — a "nothing found" result for a routine, expected-to-usually-be-clean check doesn't need a permanent artifact, and logging every clean sweep would bloat the trail past the point of being useful. (This is a deliberate exception to the project's general "every decision needs a durable home" rule — a sweep finding nothing isn't a decision, it's the ritual working as intended.)

## Rationale

- **CLAUDE.md over a memory-system entry**, because this is a *procedure* (repeatable steps triggered by a keyword), and the project already has this exact pattern for session-closing and inbox-processing — both live in CLAUDE.md, not in the auto-memory system, which is reserved for facts/preferences/context rather than playbooks. A fresh session auto-loads CLAUDE.md with zero setup from Jonathan; the auto-memory system is for the assistant's own accumulated understanding, not a stand-in for a checklist a human should be able to read and audit too.
- **A dedicated new ADR over folding into ADR-0098**, because ADR-0098 is already a specific incident report (a stuck batch invocation) that grew 8 addenda through an adjacent but genuinely different investigation (content-quality, not invocation reliability). A ritual that will recur every few days for the indefinite future deserves its own numbered home rather than continuing to stretch an ADR whose title no longer describes most of its content.
- **Clean sweeps get no permanent document**, a deliberate carve-out from this project's usual "capture every decision" rule — logging "checked, found nothing" every few days would make the CHANGELOG/ADR trail worse at its actual job (surfacing real decisions and defects) by drowning them in routine confirmations. The chat transcript itself is the record for a clean result; only findings get written down.
- **Detector results are a data point, not a verdict**, because this session directly proved that "the detector says zero hits" and "the package is actually clean" are different claims — twice (the `\b` bug, and the same-package-swap blind spot that's still unfixed by design). The checklist's manual steps (4, 5, 6) exist specifically to catch what the automated layer is known not to.

## Alternatives Considered

- **A slash command / skill.** Would work, but this project doesn't currently have custom skills wired up, and a CLAUDE.md section achieves the same "say one word, get the full checklist" outcome with zero additional tooling to build or maintain — consistent with this session's broader conclusion (in the model-staleness discussion) that the actual risk here doesn't warrant new infrastructure.
- **Fully automating the sweep** (a cron job that runs it on every new purchase). Rejected for now: steps 4-6 are judgment calls a human-in-the-loop read still does better than a regex, per the exact detector-blind-spot findings above. Revisit if/once the model-era risk concentration (almost everything found so far is pre-2026-08-11) is confirmed to have fully resolved and the checklist can safely shrink to steps 3+7 alone.

## Consequences

**Positive:**
- Jonathan can open a fresh tab, paste a purchase notification, say "sweep," and get the exact same thorough check every time — no re-explaining the methodology, no risk of a step being forgotten because a session started cold.
- The checklist is itself durable and auditable (this file), not locked inside one conversation's memory.

**Negative:**
- Still requires a human judgment pass (steps 4-6) — not a zero-effort automated gate. Acceptable given the "Alternatives Considered" reasoning above.
- Clean-sweep results aren't logged anywhere permanent, so there's no running count of "how many packages have been checked" without reconstructing it from chat history. Acceptable trade against not bloating the CHANGELOG/ADR trail.

## Addendum 1 (2026-08-23): step 4's "does the text mention the victim" check has the same substring-blind-spot the detectors have — found systemic, not isolated

A second sweep pass on an already-"cleared" package (Toby Wragg's *Death On The Île De Ré*, full incident in [ADR-0104 Addendum 3](0104-close-post-purchase-free-regeneration-hole.md#addendum-3-2026-08-23-victim-name-consistent-in-addendum-2-was-wrong--a-re-purchase-notification-sweep-on-the-same-package-caught-a-real-leak-addendum-2s-spot-check-missed)) found a victim-name mismatch that an earlier sweep's step-4 check had explicitly signed off as clean. The gap: checking whether a document *mentions* the victim's name (e.g. "does this character's background contain 'Margot'?") passes even when the actual rendered text differs — `"Margot/Marc Dubois"` contains `"Margot"` as a substring. Step 4 above is now reworded to require exact-string comparison, not presence-checking.

Checked whether this was specific to Toby's package (which had a rocky, manually-triggered generation history) or systemic: sampled 11 completed, live customer packages with a detectable dual-gender victim name in `master_context`. 3 of 11 (27%) had a real parent/child mismatch of this exact shape, not just Toby's — **Behind The Mask: Death At The Gilded Circle** and **Death At The Velvet Rose** were both live and delivered with the same defect, found and fixed the same session (detail in the ADR-0104 addendum linked above). Neither of the two automated detectors (`list_packages_with_meta_text_leak`, `list_packages_with_victim_mismatch`) caught any of the 3 — both have the identical substring-presence blind spot.

**Not done here:** a full audit of every completed package (only 11 with a cleanly-regex-extractable dual-name victim were sampled), and no pipeline-level fix (resolving the victim's name once upstream of both parent and child generation, or a new detector that diffs the exact victim string across documents). Both are reasonable follow-ups if this pattern shows up again on a future sweep.

## Addendum 2 (2026-08-25): `detective_script`'s REVEAL section can hardcode a fixed culprit in a `mystery_style='character'` package, and `list_packages_with_slip_culprit_leak()` doesn't check it

A sweep on Jaclyn's "Operation: Thirty & Murdery" (30 players, `has_accomplice=true`, conversation `624ad4af-3b5b-43d8-8f25-7df919ef153f`) found a real G8 violation (`docs/generation-guardrails.md`): `detective_script`'s "## THE REVEAL" section explicitly named a specific murderer and accomplice ("The killer is **Dr. Kai Okonkwo**... **Dr. Westley Kimura**, I believe that someone was you") in a `mystery_style='character'` package, where G8 requires "NO FIXED CULPRIT: the murderer is selected by slip-draw at the table... do not designate, name, or imply a pre-assigned murderer anywhere." Everything else in the package was structurally correct for the slip-draw mechanic — `game_overview` correctly says "the culprit is randomly selected at game time," every character has full innocent/guilty/accomplice round-script branches, and `character_role='suspect'` for all 30 (no DB-level fixed role) — only `detective_script`'s prose reveal broke the pattern.

`list_packages_with_slip_culprit_leak()` (migration `20260810_add_is_test_flag_and_harden_health_check_detectors.sql`) returned zero hits on this package and does not catch this shape: it only scans each character's static `secret`/`secrets` field for confession-style phrasing (`'you (poisoned|killed|murdered|...)'` + `'hide/conceal ... guilt'`), the exact shape of the original "Adelaide Crane" incident this detector was built for. It never scans `detective_script` for a named, fixed culprit. `package_completion_blocking_defects()` and `list_packages_with_meta_text_leak()` also don't catch it — this is a distinct defect class from both.

**Fixed directly** (zero-cost text edit, no Anthropic call): rewrote just the REVEAL section's culprit-naming sentences to use the same host-fill-in bracket convention already established elsewhere in the same document (e.g. `*[Read this when guests are gathered...]*`) — `**[GUILTY PLAYER'S NAME]**` / `**[ACCOMPLICE PLAYER'S NAME]**`, with explicit host stage directions to name the actual slip-drawer and connect their real motive in their own words. Verified: no hardcoded names remain (`detective_script ~* 'Dr\. Kai Okonkwo|Dr\. Westley Kimura'` → false), and the edit doesn't trip `list_packages_with_meta_text_leak()`'s bracket-pattern markers (correctly, since this is the legitimate host-fill-in convention step 5 already asks sweeps to distinguish from a leak).

Also found and fixed in the same sweep: **Cypress/Celine Beaumont was missing entirely** (no `mystery_characters` row — the health-check alert's "Missing Characters" list) despite `generation_status` already reading `'completed'`; the completion gate's `package_completion_blocking_defects()` only validates existing rows, so a wholly-missing row doesn't block completion at all, meaning a "ready" email can go out (and did, `ready_email_sent_at` predates the recovery here) while a paid-for character is completely absent. Manually re-fired the same recovery webhook the automated system uses (packages that flip to `'completed'` drop out of the `needs_review` sweep loop that would otherwise retry them) — row created, but her `round2_guilty`/`round3_guilty`/`round4_guilty`/`final_guilty` branches came back empty across two consecutive attempts (the automated 2-attempt cap is now exhausted), a persistent-looking failure possibly the same class as the Sylvie/Sylvain Dubois content-filter block noted elsewhere in this repo. Left as-is: with the REVEAL fix above, her guilty branch is never actually handed to a player in this package's as-delivered gameplay, so the practical impact is nil unless the Recast/reassignment feature is used on her later.

**Not done here:** no new detector was built for the `detective_script`-names-a-fixed-culprit shape, and no corpus-wide sweep was run to check whether other live `mystery_style='character'` + `has_accomplice=true` packages have the same reveal-hardcoding pattern (this incident is the first confirmed instance). Both are reasonable follow-ups if this shape shows up again.

## Addendum 3 (2026-08-25): root-caused and fixed Cypress's empty guilty branch — a genuine Claude output-formatting failure, not truncation, not content-filtering

Follow-up closing the "left as-is" item from Addendum 2. Root-caused via the Make API's execution log (module 513's actual output bundle for the exact failing run): Claude's response had `stop_reason: "end_turn"` and used only 2360 of the 5000 available `max_tokens` — ruling out truncation entirely — but the JSON itself was genuinely malformed: the last field's string value ended cleanly on a finished sentence ("...I collected one she never expected to pay.") with no closing `"` or `}` anywhere after it. The model simply stopped without closing the JSON structure it was still inside, despite the prompt's `critical_json_rules` block explicitly instructing it to verify the closing brace and quote are present before emitting. This is why all 5 fields from that one call (`round2Guilty`, `round3Guilty`, `round4Guilty`, `finalGuilty`, `revealConfessionGuilty`) came back empty together — `parse-claude-json`'s parse is all-or-nothing, so one malformed call zeroes out everything it was responsible for, even though 4 of the 5 fields' content was already complete and well-formed at the point the model stopped.

**Fixed**: added a new, deliberately conservative final sanitization stage to `parse-claude-json` (`attemptCleanTrailingCutoffRecovery`) that detects this exact shape — still inside an open string at end-of-input, with the trailing text ending on sentence-terminal punctuation (`.`/`!`/`?`) — and closes the string plus any still-open braces/brackets before retrying the parse. Deliberately does NOT recover a cutoff that doesn't end cleanly (mid-word, no terminal punctuation): that shape is far more likely a genuine `max_tokens` truncation, and silently "closing" it would ship a visibly cut-off sentence instead of correctly falling through to the existing empty-field handling (which the missing-content detectors and auto-recovery loop already cover safely). Verified against the actual real failing payload (extracted via the Make API, not reconstructed by hand) before deploying — recovers all 5 fields correctly; a synthetic mid-word truncation and normal valid JSON both still behave exactly as before. Deployed (`parse-claude-json` v10, `verify_jwt` preserved) and re-verified against both cases on the live endpoint post-deploy.

Not done: Cypress's own character row on the original package still has the empty guilty branch (this fix prevents the *next* occurrence, it doesn't retroactively backfill her — left alone since, per Addendum 2, her guilty branch is never actually used given the detective_script fix). Also not done: whether this exact "clean end-turn, unclosed JSON" shape has affected any *other* character/package historically, silently absorbed by the existing 2-attempt auto-recovery retry without ever surfacing — no historical sweep was run for it.

## Addendum 4 (2026-08-28): a dual-name-shorthand gap in the Recast scrub's `nameVariants()` survives even a "verified" removal — found via a package with a broken alibi, a translated-away-and-back-to-life red herring, and an unrelated full-package language bug

Sweep on rsappel17@gmail.com's "De Bittere Verjaardag: Moord In Villa Limoncello" (`eb0f3f1d-c404-49bb-8c7c-6daa135faa15`, Dutch, detective-style, 18→14 players via a paid 4-character Recast batch) found three separate, real defects layered on top of each other.

**1. Recast's `nameVariants()` (`adapt-mystery-apply/index.ts`) never generates the bare dual-first-name shorthand — e.g. "Jules/Julia" without the surname "Bergman" — that this generator's own prose uses constantly for casual references.** It only pairs each half of a dual-gender name with the surname ("Jules Bergman", "Julia Bergman"), plus the literal full string, plus a bare-surname fallback. Every one of Jules/Julia Bergman's 14 residual mentions after her "verified" removal used exactly this uncaught bare-first-name form — including **Robin Van Zuren's entire alibi for Round 3, Round 4, and the Final Statement**, built on "I was dancing with Jules/Julia the whole time," a person no longer in the game. Daan/Dana De Vries and Parker/Petra Lindholm (verified in the same batch) had smaller versions of the identical gap: 2 residual mentions each, one of them (Alex/Alexandra Dubois's Round 4 alibi, "I was with Jordan and Parker") load-bearing in the same way. `detective_script` also never got Jules/Julia's "could not attend, cleared" host note, despite the append being unconditional in code — a second, distinct gap in the same removal that a same-day sibling session's investigation (see [ADR-0088's 2026-08-28 addendum](0088-guest-dropout-multi-character-and-reassignment.md#addendum-2026-08-28-verify-step-never-got-the-sibling-batch-exclusion--false-positive-rollback-on-a-real-4-character-batch)) did not touch — that addendum fixed a different bug (the verify step's missing sibling-batch exclusion, which had caused Dr. Sasha/Sascha Kowalski's removal to roll back).

**2. That same sibling-batch-exclusion fix's "verified clean on retry" claim for Dr. Sasha/Sascha Kowalski was itself a false negative from the identical `nameVariants()` gap.** Sasha was a major red-herring thread — a patent-theft motive referenced as a comparison point in nearly every other character's reasoning about who else had a reason to want Hendrik dead. Manually re-scanning the live package after that addendum shipped found **26 residual "Sasha/Sascha" mentions across 12 of the 14 remaining characters**, including Casey Morrison's entire Round 4 alibi ("I was with Dr. Sasha/Sascha discussing root disease resistance") — the same broken-alibi shape as Robin/Jules, just larger. The verify step's blanket reference scan uses the same `variantRegex` as the scrub itself, so a name shape the scrub can't catch, the verify step can't catch either — "verified" and "verified clean on retry" are not independent confirmations of the same fact, they're the same regex asked twice.

**3. Unrelated to Recast entirely: 100% of the package's character content was in English while `game_overview`/`detective_script`/`master_context`/`evidence_cards` were correctly in Dutch**, matching the customer's actual concept-chat language. Root cause in `mystery-webhook-trigger/index.ts`: the child-generation scenario resolves its explicit language parameter from `profiles.language` (an account-level UI setting) rather than from the conversation's actual demonstrated language — this customer's profile read `"en"` despite every message in the concept chat being in Dutch. `evidence_cards` also had a mid-sentence English/Dutch template leak ("Each card pairs met een forensisch beeld").

**Fixed:** manually rewrote Robin Van Zuren's alibi to drop the dependency on Jules/Julia (falls back to the generic "dozen people"/"tennis club guests" corroboration already present in the same text, rather than inventing a new named witness), patched the Daan/Parker/Jules residual mentions, added Jules/Julia's missing detective_script note, then ran one combined Claude Sonnet 5 pass (approved cost estimate ~$0.84, actual $1.63) that translated all 14 characters' content to Dutch **and** rewrote every Sasha/Sascha reference to read coherently without her — verified afterward at 0 residual mentions of all four removed characters beyond their single legitimate host notes each. Fixed the evidence_cards template leak with a direct text edit.

**Not done here (at the time):**
- The `nameVariants()` code fix itself.
- The child-generation language-resolution root cause (ADR-0112 proposes it).
- A corpus-wide check of whether other completed Recast removals have the same undetected residual-mention shape.

All three follow-ups above were completed the same night — see Addendum 5.

## Addendum 5 (2026-08-28/29): fixed `nameVariants()` and a second, more severe bug it was masking; corpus sweep found a live customer's package with the actual murderer and accomplice still haunting every remaining character's dialogue

Following Addendum 4, Jonathan asked whether this had been addressed systemically. It hadn't — Addendum 4 fixed one package's data, not the code. Sequencing agreed: fix `nameVariants()` now (narrow, low-risk), propose the language-resolution fix as an ADR rather than code it directly (a real design decision, not a patch — see [ADR-0112](0112-child-generation-language-should-follow-the-conversation-not-the-account.md)), and run a corpus-wide sweep of every `mystery_adaptations` row with `status = 'verified'` before trusting that status again.

**Code fix 1 — `nameVariants()` bare dual-first-name shorthand.** Added the missing variant identified in Addendum 4 (`${before}/${altToken}`, e.g. "Jules/Julia"). Deployed `adapt-mystery-apply` v14, `verify_jwt: true` preserved.

**Corpus sweep found 15 total `verified` Recast rows across 3 packages.** Package `a0a985a9` (9 dual-named removals, 2026-08-20) checked clean — an initial alarm on 2 characters was a false positive in the sweep's own ad-hoc scan (the surname "Cross" coincidentally matched the common word "cross" in an unrelated sentence, a known and already-accepted risk of the bare-surname fallback, not a real leak). Our own package (`eb0f3f1d...`) was already clean from Addendum 4.

**Package `c28e31f6` ("El Último Brindis De Bellanotte," Spanish, detective-style, requested_by_email `esj@salgados.net`, 2 removals completed 2026-08-22) was badly broken, and not from the same bug.** `dropBlocksTargeting()` — the function responsible for dropping question/rumor blocks addressed to a removed character — has a name-shape regex hardcoded to English:

```
const nameShape = /^(?:\d+\.\s*)?\*\*(?:To|About)\s+([^:*]+):\*\*|^\*\*([^*]+)\*\*\s*-/;
```

It only recognizes `**To Name:**` / `**About Name:**`. Spanish uses `**A Nombre:**`. The regex never matched, so `target` was always `null`, and the code's own documented fallback for that case is "keep the block." **This means block-level removal has silently done nothing for every non-English Recast removal that uses this shape, since it shipped** — not a Spanish-specific bug, a bug for any of the 12 non-English locales this product ships in. Effect on this package: **39 undropped question/rumor blocks across all 13 remaining characters**, every one still instructing guests to interrogate two people no longer in the game.

It got worse on inspection. The two removed characters — Matilde de Monteverde and Aniceto de Monteverde, a married couple — were **the murderer and accomplice**, reassigned per ADR-0088 to Carlota de Villaclara (now murderer) and Gaspar Mondragón (now accomplice). Both reassignments had logged `"prose-smoothing pass errored (Unterminated string in JSON...) — deterministic-only output used"` — the Claude polish step failed and fell back to raw mechanical output; a spot check found the fallback content (Carlota's full murder confession) was actually complete and well-written, so this particular failure mode degraded gracefully. Separately, and worse: **Matilde and Aniceto were still referenced as live suspects in prose across 9 of the 13 remaining characters** — one character's final statement directly accused "Matilde" by name; several alibis referenced being "with Aniceto"; Aniceto never received a "could not attend" host note at all (Matilde did, so this wasn't the same missing-note gap from Addendum 4 — a third, independent gap). This package had been delivered to a real customer for 6 days.

Root cause of the composite-name gap: this product also uses a second removed-character naming convention beyond the "/" dual-gender form — "Real Name - PlayerNickname" (e.g. "Aniceto de Monteverde - Luis", noted in ADR-0088's 2026-08-22 addendum for a different reason). `nameVariants()` never generated the bare "Real Name" portion (without the nickname suffix) — the ONLY form this generator's own dialogue ever actually uses. Fixed by adding that variant (`dashMatch` in `nameVariants()`), in the same deploy as the `nameShape` fix. Deployed `adapt-mystery-apply` v15, `verify_jwt: true` preserved.

**Fixed for the live package:** re-ran the corrected `dropBlocksTargeting()` logic directly against the package (39 blocks dropped, $0 — deterministic, no LLM call), added Aniceto's missing host note (matching the existing English-language template `absentParagraph()` already uses for Matilde's note in the same package — host notes are English by design regardless of package language, an easy mismatch to introduce by hand, caught before shipping), then ran one targeted Claude Sonnet 5 pass (only the 9 affected characters × their specifically-flagged fields, not full-character resends — $0.15) to rewrite the remaining prose-level Matilde/Aniceto references coherently. Verified: 0 residual mentions of either removed character beyond one host note each; murderer/accomplice role counts both exactly 1; Carlota's own edited alibi-deflection paragraph re-read for coherence and no premature self-incrimination; Genoveva's final statement — which had wrongly accused the departed Matilde by name — now describes the qualifying evidence without naming anyone, which happens to correctly (if implicitly) point at the real murderer Carlota without spoiling the reveal, an improvement over the original.

**Not done:** proactively contacting `esj@salgados.net` about the 6 days their package was live with these defects — Jonathan's call, not made here. A corpus sweep only covers `status = 'verified'` rows; `adapt-mystery-create` (the create-time player-count adjustment path, separate from `adapt-mystery-apply`) was not checked and may have its own instance of either bug if it shares any of this code.

## Addendum 6 (2026-08-30): a character with zero accusations content, invisible to `package_completion_blocking_defects()`

Sweep on "Casa Ferrel" (`704cfef9-eb77-424c-abab-8e12652c3735`, 9-player slip-style with accomplice, English, generated same-day post-Sonnet-5-upgrade). All three scoped detectors and `package_completion_blocking_defects()` came back clean. A manual per-character completeness pass (checking every content column across all 9 `mystery_characters` rows, not just spot-checking 2-3) found Grant/Gracie Whitfield's `accusations` and `accusations_pointform` both `NULL`, while all 8 other characters had both populated — meaning that character, if drawn, would have had no deflection-tip guidance for the Accusations round.

Root cause of the detector blind spot: `package_completion_blocking_defects()` operates at the package level and was never designed to validate per-character column completeness across all 9 rows individually — the same category of gap already documented for the other three scoped detectors in the main body of this ADR, just on a different table/column shape. No corpus-wide sweep was run to check whether other packages have the same single-character gap on this or other columns; this is the first confirmed instance.

**Fixed:** backfilled both fields directly via SQL, written to match the "IF YOU DREW THE GUILTY/CULPRIT SLIP — DEFLECTION TIPS" format and voice already established by the other 8 characters, grounded in Grant/Gracie's own background/secret (political consultant, Porto photograph leverage), established allies/rivals (Otto/Odette ally; Ren/Fiona and Iris/Ivan rivals), and their existing Round 2-4 guilty-branch scripts (bar-cart proximity, unaccounted ~20 minutes after the toast). Re-verified: all 9 characters now have both fields populated. No detector or pipeline code was changed — treated as a one-off content gap in a single package rather than a systemic bug, since this is the first occurrence and the mechanism that dropped it is unknown.

**Not done:** no corpus-wide sweep for other packages with the same per-character null-content shape; no new automated detector for per-character content completeness.

**Update (same day):** both gaps closed properly. See [ADR-0113](0113-completion-gate-check-all-character-fields.md) — expanded `validate_package_characters()` to check the full field set instead of just `description`/`character_role`, closing the detector blind spot named above. Testing that fix against this already-"fixed" package found a *second* gap the manual sweep had missed: Grant/Gracie Whitfield was also missing `rumors`. Backfilled the same way; package now fully clean under the new systematic check.

## Addendum 7 (2026-08-30): ADR-0113's own field list was also hand-picked — found a genuinely new bug class (`secret` vs `secrets`), plus the sweep's comparison method has a structural blind spot of its own

Jonathan asked, reasonably, whether the Addendum 6 miss was itself systemic — it was: the "manual completeness check" that missed `rumors` was built from a remembered list of fields, not the actual `information_schema.columns` output, even though that full list had already been pulled once earlier in the same session for an unrelated lookup. Checking whether ADR-0113's own (larger, but still hand-picked) field list had the same flaw found it did: `_pointform` variants, `quick_reference`, `reveal_confession_guilty`/`reveal_confession_accomplice`, and `round{N}_questions` were never covered by the trigger's check.

Auditing those turned up a real, previously invisible bug rather than just a completeness gap. `mystery_characters` has both `secret` (singular, text) and `secrets` (plural, jsonb array) — the two live customer-facing render paths (`src/components/MysteryPackageTabView.tsx`, `src/pages/CharacterAccess.tsx`) both read `secret`; the only component reading `secrets` (`src/components/CharacterDetailView.tsx`) is never imported anywhere in the app. 39 characters corpus-wide have real secret content sitting only in the dead-code field, invisible to every customer despite passing an `IS NOT NULL` check on `secrets`.

**The more important catch:** finding this required checking for *uniform* nulls (every character in a package missing the same field), a shape distinct from the partial nulls (some characters have it, some don't) that both this ADR's manual sweep and ADR-0113's trigger check for. The "some vs. others" comparison method is structurally blind to a field that's wrong for 100% of a package's characters — it looks internally consistent. Checking directly found **12 whole packages with `secret` null on every character**, a count the partial-null method would never have produced on its own. 6 of the 12 are real paid packages (one row was internal test data): 3 (`MASKS OF DECEPTION`, `Murder at the Covid Arms Music Studio`, `MURDER ON THE WAVERLEY - 1962`) had the actual content sitting in `secrets`, recoverable with a zero-cost column copy; 2 (`The Great Candy Caper`, `Lights, Camera, Murder!`) have no secret content anywhere and would need real regeneration.

**Fixed:** backfilled `secret` from `secrets->>0` for the 3 packages with recoverable content (20 characters total, $0, no LLM call — verified via `RETURNING` on the UPDATE). Added the uniform-vs-partial-null distinction and the duplicate-column/frontend-usage-check practice directly into this ADR's checklist (steps 7-8 above) so future sweeps check both shapes and don't assume a populated "twin" column means the customer actually sees the content.

**Not done:** the 2 packages with no recoverable secret content anywhere (bundled into the broader historical-backfill discussion, age-gated per Jonathan's guidance — see the ADR-0113 follow-up); no `_pointform`-consistency investigation yet (checked once, saw a real-looking divergence — 53% populated for `introduction_pointform`/`rumors_pointform` vs. 17-23% for the guilty-branch/`accusations_pointform` fields — but didn't determine whether that reflects genuine per-field gaps or just which fields customers who requested point-form scripts actually asked for; flagged as a separate follow-up, not concluded here); no corpus-wide check for other duplicate-column pairs with the same live/dead-path split as `secret`/`secrets`.

## Addendum 8 (2026-08-31): package itself clean; found and fixed a 9-day-old systemic pointform gap on `reveal_confession_*` — closes the open thread from Addendum 7

Sweep on "The Workshop Of St. Nick" (`7ba1372d-9f16-4b21-a107-c0194370218d`, 10-player slip-style with accomplice, English, purchased same-day post-Sonnet-5-upgrade). All three scoped detectors and `package_completion_blocking_defects()` returned clean; manual victim-name cross-check, full `detective_script` read, evidence-card read, and a 2-character full-content spot-check all passed. `host_guide`/`timeline`/`preparation_instructions` null is the known 2026-04-25 static-template pattern, not a gap.

While spot-checking character content, found `reveal_confession_guilty_pointform`/`reveal_confession_accomplice_pointform` null on all 10 characters despite the source `reveal_confession_guilty`/`_accomplice` prose being fully populated and the customer having explicitly chosen `script_type='both'`. This is the exact gap Addendum 7 flagged as "checked once, saw a real-looking divergence... didn't determine whether that reflects genuine per-field gaps" — now confirmed as a genuine gap, not a customer-choice artifact. Corpus check: **13 distinct paid packages** affected, **152 of 164** characters with `reveal_confession_guilty` content missing its pointform sibling (93%), **110** missing `reveal_confession_accomplice`'s, going back to at least 2026-08-22 in the sample (the field itself shipped 2026-08-11, commit `6553b96`, ADR-0067).

**Root cause — corrected mid-investigation, worth recording since the first hypothesis was wrong and nearly became the shipped fix.** Initial read of the live Make.com blueprint (`Child (Unified)36`, fetched fresh via the Make API and confirmed byte-identical to its `temp-files/` export) found the `generate-pointform-summaries` call sitting visually before the `reveal_confession_*` upserts and concluded this was a sequencing bug. That was checking the wrong route — module 413, the one inspected, lives on route0 (`has_accomplice=false`, no `reveal_confession` fields at all). Route1 (`has_accomplice=true`, where this bug actually lives) has *two* pointform calls, both already positioned correctly, firing after the fields they'd need are written (525 after the guilty-branch upsert, 521 after the accomplice-branch upsert — the guilty upsert bundles `round2/3/4_guilty`, `final_guilty`, and `reveal_confession_guilty` together in one write, so by the time 525 fires all of them already exist). Confirmed directly: for the same character, in what has to be the same 525 call, `round2_guilty_pointform` came back populated while `reveal_confession_guilty_pointform` came back null — same batch, same call, one key silently missing from the model's JSON response. Not a timing bug; a Haiku 4.5 instruction-following gap on confession content specifically, the same failure family ADR-0074 already found and fixed (via a Sonnet 5 model swap) for a sibling call in this same pipeline. Caught and corrected before any blueprint edit was made or proposed as final; flagged the correction to Jonathan before proceeding rather than shipping the wrong fix.

**Fixed:** `supabase/functions/generate-pointform-summaries/index.ts` — split the single combined summarizer call into two: `MAIN_FIELDS` stay on Haiku 4.5 unchanged, `REVEAL_FIELDS` (`reveal_confession_guilty`, `reveal_confession_accomplice`) now get their own call on Sonnet 5, run in parallel via `Promise.all`. Two landmines guarded proactively rather than found live, both already documented in this codebase for Sonnet 5 (`regenerate-child-content/index.ts`, ADR-0074 lineage): Sonnet 5 400s on any non-default `temperature`, and needs `thinking: { type: 'disabled' }` explicitly or `content[0]` comes back as a non-text block. Also ported `regenerate-child-content`'s `escapeControlCharsInStrings()` repair pass as a fallback before the parse throws — the bullet-point output here (multiple bullets joined by literal `\n` inside one JSON string value) is exactly the shape that trips Sonnet 5's occasional raw-newline-instead-of-`\n` emission. Deployed `generate-pointform-summaries` v8, `verify_jwt: true` preserved (confirmed via the Management API, not assumed from the deploy command's own success message).

**Backfill completed later the same session.** Jonathan confirmed the packages were worth backfilling once he saw they were 0-26 days old (not the 130-333-day "party's already happened" precedent this project has declined to backfill before) — approved 145 calls, ~$1.50-2.50, after being shown the exact scope and per-call cost math. Ran as a standalone script (not the deployed edge function) calling only the Sonnet 5 `REVEAL_FIELDS` path directly, deliberately avoiding re-invoking the function's `MAIN_FIELDS` Haiku call, which would have re-spent on every already-good pointform field on every character touched — a real gap between what was quoted and what invoking the actual function would have cost, caught before running it. Required three passes (parallel-fetch/Node `undici` connection flakiness in the execution sandbox, unrelated to Anthropic — confirmed via direct `curl` succeeding when Node's `fetch` was failing at a high rate; fixed with a per-request `AbortController` timeout + retry-with-backoff, and the script re-queries "still null" fresh each run so it's naturally idempotent/resumable) — final pass cleaned up 2 characters where even Sonnet 5 dropped one of the two reveal fields from a combined response, the same failure mode the fix targets, just far rarer post-fix. **Result: all 145 characters across the 11 real packages backfilled, 0 remaining** (verified via direct query, not the script's own self-report) — the only residual nulls are in 2 unrelated internal test packages, correctly out of scope. Actual spend ≈ $1.65 across all passes (below the $1.50-2.50 estimate). Spot-checked written content for voice/coherence before declaring done.

**Not done:** no corpus-wide check of whether any *other* field in the `_pointform` family has a similar (if less severe) Haiku-drop rate — Addendum 7 flagged a 53% vs 17-23% populated-rate divergence across pointform fields generally and didn't conclude whether that's a per-field generation gap or just which customers asked for `pointForm`/`both`; today's finding confirms at least one real generation-side cause exists in that family, so the rest of that divergence should be treated as still open, not resolved by this fix.

**Follow-up the same session:** Jonathan asked why he'd received the "Generation Display Issue" alert email for this package at all, given the sweep found it fully clean. Pulled the actual `notify-generation-issue` function logs for the exact window (via `mcp__claude_ai_Supabase__query_logs`, not guessed) and found a second, independent, real bug — see the note appended to this same addendum below rather than a new one, since it's the direct consequence of investigating this same package's alert.

**Bonus finding, same session: `notify-generation-issue` false-alarmed on this exact package** — fired 3 times in under 4 minutes (08:36:29, 08:36:41, 08:40:02 UTC). The first two, with the package still 4-5 characters short, correctly self-suppressed via the existing `awaiting_empty_character_recovery` gate. The third fired a real alert to `support@mysterymaker.party` at 08:40:02 — **two minutes after `generation_completed_at` (08:38:09) and four minutes after the customer's own ready email had already gone out (08:36:26).** Root cause: that suppression gate requires `recoveryTargets.length > 0` (characters still empty *right now*) to conclude "recovery looks clean" — once generation finishes and nothing is empty anymore, that count is 0, the gate can't match, and the function falls through to its default of alerting. Finishing cleanly made it *more* likely to page a human, not less — the identical inversion ADR-0111 already fixed for `missing_round_content`, just never extended to this function's own `empty_character_content`/`missing_character_row` gate.

**Fixed:** `supabase/functions/notify-generation-issue/index.ts` — added `isFullyComplete` (package's `generation_status.status === 'completed'` AND zero empty/missing characters right now) as an alternate way to satisfy the "recovery looks clean" gate, alongside the existing `recoveryTargets.length > 0` mid-recovery case. Deliberately requires `generation_status.status === 'completed'`, not just "nothing empty this call" — a package stuck `in_progress` for an unrelated metadata reason with otherwise-healthy content should still alert; only a package the system itself considers done gets the pass. Deployed v28, `verify_jwt: true` preserved (confirmed via the Management API).

**Not done:** no backfill/retro-notification correcting the false alarm already sent (nothing to undo — it was informational, not customer-facing); no audit of how many other packages hit this same race historically (the log query only covered this one incident's window; a corpus-wide check would need querying `function_logs` for every `"Generation issue notification sent"` line and cross-referencing each package's `generation_completed_at` against its `last_notified_at`, not done here).

## Addendum 9 (2026-08-31): third `notify-generation-issue` false-alarm class this week — a capped character was actually still in flight

Sweep on a "New Purchase!" notification for Hannah Winter's second mystery (`425fdbd5-5430-4f1b-a3fd-eb6e150d980b`, "The Person Who Died Wasn't A Stranger", 6 players) that arrived bundled with a "⚠️ Generation Display Issue" alert: 5/6 characters, `Juniper/Julian Hale` missing entirely, "Auto-Recovery Capped ... This needs manual intervention -- it will not retry itself." By the time this was investigated, `mystery_packages.generation_status` read `completed`, all 6 characters existed with fully populated content (`Juniper/Julian Hale` included — background, secrets, all 3 round scripts, final statement, all present and coherent), and `game_overview`/`detective_script`/`evidence_cards` were all present. No manual action was actually needed — the alert's own "will not retry itself" claim was wrong.

**Root cause, confirmed directly from `auto_remediation_log`:** two recovery attempts were dispatched for `Juniper/Julian Hale` **9 seconds apart** (`15:49:55.453` and `15:50:04.444`) — both well within the character's own normal generation latency (it completed cleanly at `15:50:28`, ~30s after the first dispatch, ~24s after the second). A third invocation of this function, running sometime in that ~24-second window before either attempt had landed, counted 2 attempts already logged against `MAX_ATTEMPTS_PER_CHARACTER = 2` and declared the character permanently capped — alerting immediately, before the in-flight attempt(s) it had itself just fired (or a near-simultaneous sibling invocation had fired) got a chance to succeed. This is the third distinct false-alarm-class bug found in this exact function this week (ADR-0111's `missing_round_content` gap, this ADR's Addendum 8 `isFullyComplete` gap, now this one) — each was a different suppression condition failing to account for async recovery work still genuinely in progress at the moment of the check.

**Fixed:** `supabase/functions/notify-generation-issue/index.ts` — `characterAttemptInfo()` now also returns the most recent logged attempt's timestamp; a character whose latest dispatched attempt is under `RECENT_ATTEMPT_GRACE_MS` (90s — comfortable headroom over the ~30s a real character regeneration takes) old is left out of every bucket for that invocation (not capped, not skipped, not double-fired) rather than judged before the async work it triggered could land. Deployed `notify-generation-issue` v29, `verify_jwt: true` preserved (confirmed via `supabase functions list`).

**Not done — flagged, not fixed:** the actual root cause enabling all three of this week's false-alarm bugs is that this function gets invoked multiple times within seconds of each other for the same package (confirmed here: 9 seconds; Addendum 8's case: 3 times in under 4 minutes). Each fix so far has patched one more way a too-frequent re-check can misjudge in-flight work, rather than addressing why the re-checks overlap in the first place — most likely multiple Make.com child-scenario executions each independently completing and re-validating via the ADR-0108 "revalidate on every write" trigger. A per-package short-lived lock/debounce (e.g. skip processing if another invocation ran for this package in the last N seconds) would close this class of bug at the source instead of one symptom at a time, but that's a concurrency-semantics change to a production alerting path and wasn't made without discussing it first. Also not done: a corpus-wide check for how many other historical alerts hit this same capped-in-flight race (would need `function_logs` + `auto_remediation_log` timestamp cross-referencing per package, not done here — same gap Addendum 8 already flagged and left open for its own bug).

**Corroborated independently on the Make.com side, same day.** Jonathan received Make.com scenario warning notifications for this same package. Checked via the Make API (`scripts/testMakeConnection.mjs`'s auth pattern, `MAKE_API_TOKEN`/`MAKE_ORGANIZATION_ID`/`MAKE_REGION` from `.env`): the Child scenario's Incomplete Executions queue (`GET /dlqs?scenarioId=9061052`) held two unresolved entries, both today, both `[409] Key (package_id, character_name)=(504d973c-..., Juniper/Julian Hale) already exists — duplicate key value violates unique constraint "mystery_characters_package_character_unique"`, timestamped 15:50:35 and 15:50:42 — i.e. shortly after the second redundant re-fire's Child execution (dispatched 15:50:04.397, Make execution status `2`/warning, only 8 of the normal ~19 operations) tried to insert a character row the *first* re-fire's execution had already created seconds earlier. Same root cause, two independent symptoms: Postgres saw the race as a false "capped, needs a human" signal; Make saw the same race as a duplicate-insert error. Parent scenario's DLQ was clean for the same window — this is isolated to the Child scenario's character-recovery path, consistent with the rest of this addendum. The two DLQ entries are moot (the character exists correctly; retrying either would just hit the same 409 again) — left for Jonathan to dismiss manually rather than clearing them via the API.

## Addendum 10 (2026-08-31): closes Addendum 7's open `_pointform` divergence thread — mostly a rollout-date artifact, the one real outlier is a boilerplate-text non-bug

Follow-up requested after Addendum 8: whether Addendum 7's "53% vs 17-23%" populated-rate divergence across `_pointform` fields reflects a genuine generation-side gap (same failure family as the `reveal_confession_*` bug) or just which fields customers actually requested. Ran real corpus queries against the live DB (project `mhfikaomkmqcndqfohbp`) rather than re-eyeballing — no code changed, no Anthropic spend.

**Reproducing Addendum 7's numbers first exposed a scoping problem in the original eyeball check, not a generation bug.** Querying every `mystery_characters` row belonging to a real, paid, `script_type='both'` conversation (871 characters, all-time) reproduced a similar-looking split (introduction/rumors ~56%, guilty-branch/accusations ~25-35%) — but breaking the same query out by package `created_at` in weekly buckets showed why: every character from a package created before 2026-04-20 has **zero** `_pointform` fields populated, of any kind — not a low rate, an absolute zero, because the point-form summarizer feature didn't exist yet for those packages (confirmed directly: three sampled pre-rollout characters had `introduction_pointform` through `final_guilty_pointform` all literally `null`, no partial pattern at all). The feature rolled out mid-week of 2026-04-20 (17/65 that week) and reached ~100% "any pointform populated" by 2026-04-27 onward. Since roughly half the all-time corpus (398 of 871 characters) predates the feature entirely, blending it into one aggregate rate mechanically drags every field's "populated" percentage down and manufactures the appearance of field-specific divergence that isn't real per-field behavior — it's a rollout cliff.

**Re-running the same field breakdown scoped to `mp.created_at >= '2026-04-27'` (473 characters) collapsed nearly the entire divergence.** Every field — `introduction`, `rumors`, both `round{2,3,4}_script`/`final_statement` (detective-style), all twelve `round{2,3,4}/final_{innocent,guilty,accomplice}` role-variant fields, and both `reveal_confession_*` fields (100%, confirming Addendum 8's fix and backfill are holding) — sits at 97-100% populated wherever the source field has content. **`accusations_pointform` is the one real outlier: 213/473 (45.0%).**

**That outlier is not a generation bug either.** Same-call diagnostic (the exact method that caught `reveal_confession`'s Haiku drop): sampled characters missing `accusations_pointform` and checked whether `introduction_pointform`/`rumors_pointform` — siblings in the identical single `MAIN_MODEL` batch call — succeeded for the same character. They did, every time. So this isn't "one field silently dropped mid-call" like the `reveal_confession` bug; it's field-specific. Reading the actual `accusations` source text for the 260 characters missing the pointform sibling explained it directly: **254 of 260 (97.7%)** carry the generator's own generic "improvised in-the-moment" boilerplate (`## ACCUSATIONS` / `### YOUR ACCUSATION` / *"During the accusations phase, name one suspect and give a brief reason..."*, confirmed present in English, Spanish, and French variants via an `ilike '%improvis%'` scan) — a stage direction telling the player to make up their own accusation at the table, not character-specific tactical content. There is nothing to bullet-point, and the system prompt's own instruction ("Capture every key tactical move... in the prose") gives Haiku no directive to summarize an instruction-to-improvise as if it were content — treating this as a correct null, not a drop, is well-supported.

Of the remaining 6 non-boilerplate misses, 5 are longer variants of the same "you might say something like..." generic coaching text (still no character-specific tactical content to bullet). Exactly **1 of 473 characters** (Maximilian/Maxine Rothschild, package created 2026-06-03) had genuine bespoke DEFLECTION TIPS content that should have produced bullets and didn't — an isolated single-character miss, not a systemic pattern (0.2% of the post-rollout corpus), well within normal noise and not the same shape as the 93%-drop `reveal_confession` bug.

**Conclusion: hypothesis (b), not (a).** The `_pointform` divergence Addendum 7 flagged is fully explained by (1) a feature-rollout date cliff inflating the apparent gap when the all-time corpus is queried without a date scope, and (2) the `accusations` field for slip-style (`mystery_style='character'`) games routinely containing genuinely non-substantive "improvise your own" boilerplate rather than tactical content — Haiku correctly declining to bullet-point that is expected behavior, not a bug. No fix proposed, no code touched, no Anthropic spend. This closes the open thread from Addendum 7.

**Not done:** no backfill decision made for the ~398 pre-2026-04-27 characters in paid `script_type='both'` packages that have zero `_pointform` content at all (predates the feature; distinct question from today's, and Jonathan's call per the project's existing age-gated-backfill precedent — see Addendum 7's `secret`/`secrets` backfill for the pattern). No action taken on the single Maximilian/Maxine Rothschild miss — isolated, sub-1% rate, not worth a targeted fix on its own.

## Addendum 11 (2026-08-31): closed the actual root cause behind Addenda 8-9 — a per-package cooldown on `notify-generation-issue`

Addendum 9 flagged but deliberately didn't fix the real root cause behind three separate false-alarm bugs this week: `notify-generation-issue` gets invoked multiple times within seconds of itself for the same package (multiple Make.com child-scenario executions each independently re-validating completion via the ADR-0108 trigger), and each invocation dispatches its own recovery re-fire with no awareness of a near-simultaneous sibling having just done the same thing. Jonathan asked for this recap explicitly, decided it was worth fixing at the source rather than continuing to patch individual symptoms, and approved the fix.

**Fixed:** new `mystery_packages.notify_last_run_at` column (`supabase/migrations/20260831_notify_generation_issue_cooldown.sql` — applied directly via the Supabase Management API's `/database/query` endpoint, using the CLI's own stored access token from the macOS keychain, since `supabase db push` hit the same long-standing local/remote migration-history drift noted in the `adapt-mystery-apply` incident and wasn't touched). At the top of `notify-generation-issue`, right after loading the package and before any character/recovery logic runs: if `notify_last_run_at` is under `NOTIFY_COOLDOWN_MS` (20s — comfortably covers both overlap gaps observed so far, 12s and 9s) old, the invocation returns immediately (`{success:true, skipped:"cooldown"}`) without touching the recovery loop at all. Otherwise it stamps the column with the current time as early as possible (before any of the slower work below) so a near-simultaneous sibling invocation sees the fresh timestamp and skips too. Nothing is permanently missed — a genuinely unresolved problem is caught by the next invocation past the cooldown, or by the existing 10-minute sweep crons that already backstop this regardless.

Deployed `notify-generation-issue` v30, `verify_jwt: true` preserved (confirmed via `supabase functions list`). Live-tested safely against Hannah's own already-`completed` second package (`425fdbd5-.../504d973c-...`) — two calls fired back to back: the first ran normally (found nothing to fix, since the package is genuinely healthy — no real recovery dispatch, no spend), the second correctly returned `{"skipped":"cooldown"}`. Confirmed `notify_last_run_at` recorded and `generation_status` unaffected by the test.

## Addendum 12 (2026-09-01): stale `generation_status` snapshot masked a completion-trigger write-order race, plus one genuine `self_directed_question` header bug — both fixed

Sweep on "Elementary, My Dear Cadaver" (`98857ef0-71d5-48a1-8c04-09be5092169c`, 12-player slip-style with accomplice, English, generated same-day post-Sonnet-5-upgrade). All three scoped detectors came back clean and the victim's name (Dame Agatha Nightshade) resolved consistently across `game_overview`, `detective_script`, and all 12 characters. `package_completion_blocking_defects()` returned one real hit: `self_directed_question.Columbolumbo Kane`.

**Bug 1 — genuine, fixed.** Columbolumbo Kane's `round2_questions` had a malformed header on its first question: `**To Columbolumbo Kane's own suspicion, directed at Ellery Quince-idink:**` instead of the `**To [Name]:**` format every other question in the package uses. Fixed with a direct string replace to `**To Ellery Quince-idink:**`.

**Bug 2 — a stale status snapshot, not 2 broken characters.** The package's `generation_status` read `needs_review` with `"emptyCharacters": 2`, but the message named only one character (the self_directed_question hit above). Re-running `validate_package_characters()`'s exact empty-character query live against current DB state returned a count of 1, not 2: only Dr. Jamie Whatsit was actually missing content — all 10 accomplice-branch fields (`round2/3/4_accomplice`, `final_accomplice`, `reveal_confession_accomplice`, and all 5 `_pointform` twins) were `NULL` while every other one of the 12 characters had them fully populated. Character `updated_at` timestamps show why the snapshot was wrong: the completion trigger fired at `20:40:11`, but Ellery Quince-idink's row wasn't written until `20:42:10` — nearly 2 minutes later. The trigger evaluated a mid-write DB state and captured a transient real gap on a second character that resolved itself before this sweep ever ran. The snapshot itself never re-computes, so it kept reporting the stale count indefinitely.

**Fixed:** Dr. Jamie Whatsit's accomplice-branch content was hand-written directly (not via `regenerate-child-content` — that function's `SUPPORTED_FIELDS` whitelist doesn't include `reveal_confession_accomplice` or any `_pointform` field at all, so even a paid call would have left 6 of the 10 fields empty; and per the standing global rule, paid Anthropic calls need explicit sign-off first regardless). Grounded in Dr. Whatsit's established clinical/exasperated voice, his existing `secret`/`round2_guilty`/`round2_innocent` fields (struck-off medical registry), his `relationships` field (protective of Bones, wary of Charlie Chandelier), and peer character Miss Muddle's full accomplice-branch content as a structural/length template. Followed the accomplice-coherence rule from `regenerate-child-content`'s own prompt (protect the murderer, never name or incriminate a specific culprit, since the culprit is drawn by slip at table-time) by casting suspicion broadly across multiple other characters rather than committing to one.

Re-verified: `package_completion_blocking_defects()` returns `null`, live empty-character count is `0`. Manually flipped `generation_status` from `needs_review` back to `completed` (a plain status write, not a bypass) — this re-triggered `validate_package_characters()` for real, and it now passes cleanly on its own, confirming the fix rather than just asserting it.

**Not done:** no corpus-wide check for other packages with the same trigger-fired-before-last-character-write race (this is a narrower, later-arriving instance of the general class ADR-0106/0108 already track — a snapshot going *stale in the safe direction* by under-counting fixed problems as still-broken, rather than over-counting broken ones as fixed, so it was not treated as urgent enough to widen scope on today's sweep); no fix to `regenerate-child-content`'s field whitelist to add `reveal_confession_accomplice`/`_pointform` support, though its absence was the reason this had to be hand-written rather than run through the standard repair path — flagged here as a real gap in the tooling, not fixed today.

## Addendum 13 (2026-09-01): first German-language sale surfaced a new bug class the existing checklist doesn't cover — inconsistent formal/informal address ("Sie" vs "du")

Sweep on "Tod Auf Der Alm 3000" (`1773bfb5-93a3-4313-865e-4b40f35d9a13`, conversation `d5d66a5b-95b2-468d-83fa-d285d044673c`, German, 4-player slip-style, no accomplice, generated 2026-09-01 20:40, well post-Sonnet-5-upgrade) — Jonathan's first paid German-language mystery. All three scoped detectors were clean, the victim's name (Dr. Kim Fusion) resolved consistently across `game_overview`, `detective_script`, and all 4 characters, and `package_completion_blocking_defects()` returned `null`. The detective script and character background/relationships/secret content read cleanly on a full manual pass.

Reading all 4 characters' round/final branch-selection headers turned up a real, previously-uncatalogued bug class: German distinguishes formal ("Sie") and informal ("du") address, and each character's headers were internally inconsistent about which one to use.

- **Stern**: introduction used formal "Sie" ("Lesen Sie dies laut vor..."), but every one of the 8 round/final branch headers ("WENN DU SCHULDIG BIST" etc.) used informal "du" — a mismatch against the character's own established voice.
- **Vance**: introduction and all 4 innocent-branch headers used formal "Sie" ("WENN SIE UNSCHULDIG SIND"), but all 4 guilty-branch headers used informal "du" ("WENN DU SCHULDIG BIST") — the mismatch isolated to exactly the guilty branch.
- **Glitch**: the Round 1 header itself was a literal unresolved placeholder — "## RUNDE 1: IHRE/DEINE VORSTELLUNG" — with the same "Ihre/Deine" duplication repeated in the relationships and secret section headers, and the 4 innocent-branch headers used yet a third, unrelated pattern ("WENN ICH UNSCHULDIG BIN" — first person, matching neither "du" nor "Sie").
- **Frost**: fully consistent informal "du" throughout every header — the only one of the 4 with no defect.

Confirmed the character monologues themselves (round/final body prose) are entirely first-person "ich" throughout for all 4 characters, with no singular "du"/"Sie" direct address in the body text — the defect was confined to the instructional branch-selection header labels, not the character's actual spoken lines. This meant a header-only fix was safe and didn't require touching or rewriting any character voice/prose.

**Fixed via direct SQL**, standardizing each character to the register established by their own introduction: Stern's 8 round/final headers converted from "du" to "Sie" form; Vance's 4 guilty-branch headers converted from "du" to "Sie" form (innocent branch was already correct); Glitch's intro/relationships/secret placeholder headers resolved to "du"-only, and the 4 "WENN ICH..." innocent headers converted to match the existing "WENN DU..." guilty-header pattern. Frost required no changes. Re-verified all 4 characters' full header set post-fix — no remaining placeholders or mismatches.

**Root cause, investigated but not fixed today:** the child blueprint's language directive (`Write ALL output in {{63.language}}... translate these into {{63.language}} rather than reproducing the English template text verbatim`) has no instruction to hold a single grammatical register consistently across a character's fields. English's "you" is register-neutral, so the directive gives the model no signal that translating "you" into German (or into French tu/vous, Spanish tú/usted, Portuguese tu/você, Italian tu/lei, Dutch je/u — the same T-V ambiguity exists in at least 6 of the 13 supported languages) needs to land on the same choice every time for one character. Each field appears to get translated somewhat independently rather than against a character-level register decision. No existing detector (`list_packages_with_meta_text_leak`, `list_packages_with_victim_mismatch`, `list_packages_with_unresolved_victim_name`, `package_completion_blocking_defects`) covers this bug class — all of it was found by manual read-through only.

Spot-checked headers on 2 other genuine non-English paid packages to gauge how widespread this is: one French package (`LA NUIT DORÉE`, `4df6139c-3351-41d3-8a43-db22b1f279dc`) was clean — consistently formal "vous" across all 8 characters' intro/round2 headers, no mixing found. One Portuguese package (`MISTÉRIO: O ÚLTIMO BRINDE DE HUGO MARTINS`, `dd2203b6-6fdf-421b-8034-1a808405709c`, generated 2026-02-04) showed a *different*, older bug — English section headers ("INNOCENT RESPONSES" / "GUILTY RESPONSES") leaking into otherwise-Portuguese content — but that package predates several already-fixed language issues (see the 2026-08-28/29 non-English regression closure) and isn't evidence about the current blueprint. A genuine Spanish package (`El Último Brindis De Bellanotte`, `c28e31f6-f821-4dc6-abc3-c555a07ad9bb`) turned out to use a different content structure entirely (`round2_innocent`/`round2_guilty` both `null` for every character — content lives elsewhere in that package's schema), so it wasn't comparable. **Inconclusive** on how widespread the register-consistency gap is beyond this one German package — sample size too small, and this is literally the first German sale, so there's no prior German package to cross-check against. Prompt hardening (an explicit "pick one register per character, formal or informal, and use it identically in every field" instruction) was proposed to Jonathan as a follow-up fix but not yet drafted into a new child blueprint version — deliberately deferred rather than blind-edited into the 150KB+ live blueprint JSON without his sign-off, per the standing "no bulk changes without showing me first" rule.

## Addendum 14 (2026-09-01): `sweep_incomplete_packages()` was catching packages mid-generation — a recurring, corpus-wide race, not a one-off; fixed with a quiet-period gate

Jonathan asked, after Addendum 12, whether the notify-generation-issue alert on "Elementary, My Dear Cadaver" had been warranted. It had — Dr. Jamie Whatsit's accomplice branch and Columbolumbo Kane's self-directed-question bug were both real — but digging into `auto_remediation_log` for the package surfaced a second, bigger problem Addendum 12 had missed: at `20:38:03`, a full **2 minutes before `generation_completed_at` (20:40:11)**, something had already flagged **six** characters as empty (not the two the final alert reported) and dispatched recovery-regeneration webhooks for all of them — Ren-fox, Chandelier, Marple-Wolfe, Ramoteapot, Noir-ish, Quince-idink. All six turned out fully populated once generation finished naturally seconds later; the recovery dispatches were pure noise, and real spend (`$0.15`/character logged in `auto_remediation_log`, an actual regeneration webhook re-fire, not a no-op).

Root cause: `sweep_incomplete_packages()`, run by the `sweep_incomplete_packages_2min` cron every 2 minutes, matches any package where `generation_status->>'status' = 'completed' OR generation_completed_at IS NOT NULL` and NOT `needs_review`. Make.com's child scenario writes `generation_status.status = 'completed'` as an early/interim signal before every character row has finished being written — the sweep's 2-minute cadence is just fast enough to catch a package in that exact window on its very first pass.

**Confirmed this is systemic, not isolated to today's package.** Queried `auto_remediation_log` for `defect_class = 'empty_character_content'` over the prior 14 days and compared each package's first flagged attempt against its `generation_completed_at`: **9 other packages** show the identical pre-completion gap, consistently ~1:40–3:47 (median ~2:00–2:10 — exactly the sweep's own cadence). This has been happening on a meaningful fraction of generations, not a rare fluke.

**Fixed:** added a quiet-period gate to `sweep_incomplete_packages()` — skip a package if any of its `mystery_characters` rows has been written to within the last 3 minutes (`supabase/migrations/20260901_sweep_incomplete_packages_quiet_period_gate.sql`, applied directly via the Supabase Management API). Mirrors the `RECENT_ATTEMPT_GRACE_MS` pattern already used in `notify-generation-issue` (Addendum 9/ADR-0111): "still being actively written" is treated as still-in-progress, not stuck. Nothing is permanently missed — a genuinely stuck/crashed package goes quiet and gets caught on a later 2-minute pass once the grace elapses. Verified the deployed function definition contains the new gate.

**Also surfaced, not part of the fix:** flipping "Elementary, My Dear Cadaver"'s `generation_status` back to `completed` during Addendum 12's fix re-triggered `trg_z_notify_package_ready` for real (it had never fired, since the package never cleanly reached `completed` before), sending the actual "Your Mystery is Ready" email to the customer. Correct now that the package is genuinely complete, but worth naming since it was a side effect of a status write rather than a deliberate send.

**Not done:** no backfill/audit of the 9 historical packages' recovery-webhook spend (each is long since naturally completed and correct; the wasted spend already happened and isn't recoverable); no change to `notify-generation-issue`'s own character-emptiness check, which is a separate code path from the sweep's gate and could theoretically hit the same race via a different caller (the ADR-0108 completion trigger) — not observed happening independently of the sweep in the 14-day corpus check, so left alone pending evidence it's a real second source.

## Addendum 15 (2026-09-02): closed the other two open items from Addendum 12/14 — a second race-condition entry point, and the `reveal_confession_*` gap in the standard repair tool

Jonathan asked to fix all three open items from Addendum 12/14 rather than leave two of them as "noted but not done." Two more fixes shipped:

**1. `notify-generation-issue` itself now has the same quiet-period gate, closing the entry point Addendum 14's sweep-cron fix couldn't reach.** Addendum 14 fixed `sweep_incomplete_packages()` racing a still-in-flight generation, but that cron is only ONE of two callers of `notify-generation-issue` — the ADR-0108 completion trigger (`validate_package_characters()`) calls it directly and synchronously the moment Make.com writes `generation_status='completed'`, even when that exact write races the same scenario's trailing character UPDATEs (confirmed: this is what actually produced the "2 characters" figure on "Elementary, My Dear Cadaver" in Addendum 12 — Ellery Quince-idink's row kept updating for ~2 minutes after the trigger fired). The trigger's `needs_review` verdict is accurate for that exact commit-time snapshot, so gating the trigger itself isn't the right fix; instead, `notify-generation-issue` (`supabase/functions/notify-generation-issue/index.ts`, deployed v31) now checks — regardless of which caller invoked it — whether any `mystery_characters` row for the package was written to in the last 3 minutes, and skips the entire cycle (alert email AND recovery-webhook dispatch) if so. Single point of protection covering both callers, mirroring the sweep's own fix.

**2. `regenerate-child-content` can now repair `reveal_confession_guilty`/`reveal_confession_accomplice`.** These two fields (the "THE REVEAL — YOUR CONFESSION" beat, distinct from `final_guilty`/`final_accomplice`) were never in this function's `PromptGroup`/`SUPPORTED_FIELDS`, nor in the DB-level `remediation_read_field`/`remediation_write_field` whitelist — the exact gap that forced Addendum 12 to hand-write Dr. Jamie Whatsit's accomplice branch instead of using the standard tool. Fixed: `supabase/migrations/20260902_remediation_whitelist_reveal_confession_fields.sql` adds both fields to the DB whitelist; `supabase/functions/regenerate-child-content/index.ts` (deployed v13) adds a new `GROUP_REVEAL_CONFESSION` prompt group (character-style only, anchored against `final_guilty`/`final_accomplice` for coherence) plus the matching `CHR_TEXT_FIELDS`/`DB_TO_JSON_KEY` entries. Deployed content verified byte-for-byte against the local file via `get_edge_function`.

**Deliberately NOT done, and why:** the `_pointform` variants of these two fields (`reveal_confession_guilty_pointform`/`_accomplice_pointform`) are still unreachable through this tool. They're generated by a completely separate function, `generate-pointform-summaries`, which is invoked directly by the Make.com Child blueprint — nothing in this repo's code calls it (confirmed via `grep -rl`). Chaining a call to it from inside `regenerate-child-content` would add a new always-on paid Anthropic call path (Haiku + a Sonnet 5 call specifically for reveal-confession fields, per that function's own comments) to a repair tool that's supposed to be a narrow field-level fix — and its gating logic (is it conditional on `conversations.script_type = 'both'`? Something else?) lives in the Make.com blueprint, which isn't visible from this codebase. Guessing wrong here risks spending on customers who never asked for point-form content. Left as an explicit open follow-up rather than wired in blind.

**Also from this pass — a corpus-wide check for Dr. Jamie Whatsit's specific defect shape (a genuinely missing accomplice branch, not a race), separate from item 1/2 above:** found exactly 2 packages corpus-wide with this pattern — "Blood And Bouquet: A Christmas Conspiracy In The Mountains" (18 players, completed 2025-12-01, 9 characters affected) and "IMG.LY Holiday Murder Mystery - 49 Players" (32 players, completed 2025-10-27, 9 characters affected). Both predate the current pipeline by months and were almost certainly already played at their respective events — unlike Dr. Jamie Whatsit's isolated single-character gap, roughly half the cast is affected in both, suggesting an old large-cast generation limitation rather than the same bug. Matches this project's established age-gated-backfill precedent (see Addendum 7's `secret`/`secrets` discussion): held for Jonathan's explicit call rather than backfilled automatically, since these are old, already-delivered packages with no clear customer benefit to touching now.

**Decision (2026-09-02):** Jonathan's call — leave both packages as-is, no backfill. Closed, not just deferred.

## Addendum 16 (2026-09-02): two same-day purchases with wrong `conversations.title` ("The Sheriff", "The Empire") — a pre-existing title-extraction race, not a new regression; data fixed, race fixed

Jonathan flagged two purchases the same day with unusually generic 2-word titles ("The Sheriff", "The Empire") and asked whether this was a new pattern, specifically because wrong titles are visible far beyond the customer's own package — the homepage's `RecentSalesPopup` widget cycles `conversations.title` to every visitor via the `get_recent_public_sales` RPC, with zero validation on the string (confirmed: `title is not null` is the only filter, no length/sanity check).

**Root cause: a pre-existing race in `extractTitleFromMessages` (`src/utils/titleExtraction.ts`), triggered live via `MysteryChat.tsx`'s per-message title write.** The extractor runs three passes per message: (1) a `# TITLE` markdown heading — the reliable signal, (2) a `title:` label, (3) a fallback that grabs any bold 2-10 word phrase starting with "The "/"A " in the first 500 chars. Titles are extracted and written **per assistant message as the chat streams**, and once a real (non-placeholder) title is set, later messages are deliberately not allowed to overwrite it — a guard added in commit `799ba16` (2026-05-19) to stop a *different* bug (a stray bold label clobbering an already-correct title).

That guard is what backfired here: in both purchases, an early assistant message contained a short bold phrase that passed the pass-3 fallback — a roster line `1. **The Sheriff** — a full suspect...` in one chat, and a clarifying question `1. **The empire** — what did the family build...` in the other — and locked in as the title *before* the real `# TITLE` heading arrived in a later message (`# DEAD MAN'S HAND: A GOLDEN GULCH MYSTERY` and `# BLOODLINE: THE LAST CHECK-IN` respectively, confirmed by reading each conversation's `approved_concept_message_id` content directly). By the time the real heading streamed in, the title was no longer a placeholder, so the correct match was silently discarded.

**Checked whether this is a new pattern:** queried every paid conversation ever for short/generic titles (word count ≤ 2, and specifically the `^The [Capitalized]$` shape) — these two are the *only* matches in the corpus, ever. The underlying race is old (no commits touched this code path in the last 7 days) and has presumably been dormant/rare because it needs a specific ordering (a short bold phrase streaming in before the real heading), but landing twice in one day appears to be coincidence, not a shift in generation behavior.

**Fixed, both parts:**
1. **Data** — corrected `conversations.title` and `mystery_packages.title` for both packages directly from their `approved_concept_message_id` content: "The Sheriff" → "Dead Man's Hand: A Golden Gulch Mystery" (conversation `9b821518-4a1e-48eb-a53a-5897d36f7992`, package `ee018906-0e4d-49d4-b54d-6719b33bfbaf`); "The Empire" → "Bloodline: The Last Check-In" (conversation `c2a6c0fd-d06d-49ff-9e61-5c43e342d612`, package `5949180b-f147-440d-9e8a-10b0ddb5b65a`). Verified via `get_recent_public_sales(10)` that the homepage widget now shows the corrected titles.
2. **Race condition** — `extractTitleFromMessages` (`src/utils/titleExtraction.ts`) now also exposes *which* pass matched, via a new `extractTitleWithConfidence()` (the old function is now a thin string-only wrapper around it, so the other three read-only call sites — `HomeDashboard.tsx`, `MysteryPurchase.tsx`, `MysteryView.tsx` — are unaffected). `MysteryChat.tsx`'s per-message write path now allows a `# TITLE` heading match (`isHeaderMatch: true`) to overwrite an existing title even when it's not a placeholder, while still blocking lower-confidence bold-fallback matches from overwriting an existing title — preserving the intent of the original May 2026 fix in the one case (pass-3 bold fallback) it was actually meant to guard against, while closing the gap in the other (pass-1 heading, which should always win).

**Deliberately NOT done:** `MysteryView.tsx`'s separate title-write effect (`updateMysteryTitle`, lines ~260-288) still gates on its own stale `isDefaultTitle` predicate (checks for the literal `"<theme> - N Players"` format) — a second, independent placeholder check that's already out of sync with `MysteryChat.tsx`'s broader placeholder logic (same paired-predicate-drift shape as prior bugs, see `[[project_gate_detector_deadlock]]`). With the `MysteryChat.tsx` fix in place, this path is now a dormant secondary safety net rather than the thing actually producing wrong titles, so left as-is rather than refactored under this addendum's scope. Flagged as a follow-up if a title-mismatch bug of a different shape shows up here again.

## Addendum 17 (2026-09-04): new bug class — a genuine "accomplice trio" story left a confessed conspirator unarrested in `detective_script`'s reveal; no existing detector or alert can see this shape

Sweep of "Lethal Mutations: Death At The Helix Institute" (conversation `11610ef0-bade-4a88-926f-679c381a03ef`, package `6958982f-1a3c-4f4a-bd20-3cc68866cc70`, 11 players, `has_accomplice=true`, `mystery_style='detective'`, `language='en'`, purchased 2026-09-04, generated same day post-ADR-0074 with no scrutiny discount warranted). All three automated detectors and `package_completion_blocking_defects()` came back clean, and Jonathan separately confirmed no `notify-generation-issue` alert fired for this package.

**All three were right to stay quiet — this isn't a data-completeness or leaked-text defect, it's a narrative-logic one, and nothing in the current detector suite checks for it.** `master_context`'s structured suspect roster explicitly designs a three-person conspiracy — Dr. Odessa Helix (murderer) is described as "mastermind of the **accomplice trio** with Nikolai/Nikki Petrov and Dr. Marlowe/Marlo Fenn," and both Nikolai/Nikki and Marlowe/Marlo carry `role: "accomplice"` in that same roster, each with a full motive/means/elimination-reason writeup. This is corroborated downstream: `mystery_characters.character_role = 'accomplice'` was correctly set on *both* characters, and both had complete, well-written, self-consistent content — including a `final_statement` where Nikolai/Nikki explicitly confesses to serving as lookout ("I agreed to help... I kept watch... I helped end a life instead of just surviving what that life had done to me").

**The break is in `master_context`'s own schema: `accompliceDetails` is a singular object, and only captured Marlowe/Marlo.** Whatever downstream step generates `detective_script`'s climactic REVEAL evidently reads from that singular field, not from the roster's `role: "accomplice"` entries — so the reveal named and arrested only Dr. Odessa Helix and Dr. Marlowe Fenn, never mentioning Nikolai/Nikki at all. The practical effect: a guest playing Nikolai/Nikki openly confesses to being part of the murder conspiracy during final statements, and then the detective's arrest a few minutes later simply never brings it up — a proven, self-confessed co-conspirator walks out of the story unaddressed. This would read as a continuity error to any table playing the game, not just a quality nitpick.

**Fixed this package by hand** — edited `detective_script`'s REVEAL section directly (SQL `UPDATE` via `mcp__supabase__execute_sql`, `replace()` on the exact matched substring, verified before and after) to add a confrontation beat for Nikolai Petrov (grounded in the roster's own elimination-reason detail — "lingering near the main floor bar... consistent with his role as lookout" — so the added text doesn't introduce new unsupported facts) and updated the arrest line from "you are both under arrest" / "what was done to you both" to cover all three. Re-read the full REVEAL section after the edit to confirm it now reads coherently start to finish. Also fixed one unrelated grammar slip found during the same read of `game_overview` ("A investor watching millions evaporate" → "An investor..."). Grepped `host_guide`, `evidence_cards`, and `hosting_tips` for other "accomplice" references — none found, so no other document needed the same fix. Grepped all 11 characters' full content for leaked brackets/placeholders — clean.

**Not determined — and not something to decide solo:** whether this is a rare one-off (a story concept that happened to grow a third conspirator from the customer's chat despite `has_accomplice` being a single boolean toggle) or a recurring gap in how `master_context` is generated whenever a customer's underlying story concept naturally implies more than one accomplice. No corpus-wide check was run for other packages with multiple `character_role='accomplice'` rows sharing one package — that's the natural next step if this recurs, following this checklist's existing precedent (see Addendum 13's German register bug) of treating a second occurrence as the trigger to decide, with Jonathan, between another one-off patch and a structural fix (e.g., teaching the detective-script-reveal generation step to read the roster's plural `role` field instead of, or in addition to, the singular `accompliceDetails`).

## Addendum 18 (2026-09-04): new bug class — "empty relationships" fallback leaks meta-commentary ("relationship matrix", "cast dynamics", "self-reference") into character sheets; confirmed corpus-wide, not a one-off, detector regex extended same day

Sweep of "Murder At The Golden Feather Awards" (conversation `97115032-8948-4810-a8ca-3a09c9a30ea1`, package `569eb956-5c73-482d-807a-b4340fd1f2c6`, 22 players, `has_accomplice=false`, `mystery_style='detective'`, `language='en'`, purchased 2026-09-04, generated same day post-ADR-0074 with no scrutiny discount warranted). All three automated detectors and `package_completion_blocking_defects()` came back clean, victim name ("Fuchsia Flambeau") was consistent across `game_overview`, `detective_script`, and every character checked, and the murderer's identity matched cleanly between `mystery_characters.character_role='murderer'` and the detective script's reveal.

**Manual spot-check of character content (checklist step 6) found a real leak the detectors missed.** 2 of the 22 characters (Hedwig/Harlan Hummingbird, and the murderer Percival/Patricia Penguin) had a bracketed fallback sentence in their "RIVALS & ENEMIES" section for the case where a character has no hostile relationships, e.g. `[No hostile relationships apply to this character per the established cast dynamics.]` — phrasing like "per the established cast dynamics" reads as leaked model reasoning about its own generation process, not in-character narrative.

**Checked whether this was a one-off before doing anything — it wasn't.** A quick `ILIKE '%[No%]%'` sweep of `mystery_characters.relationships` across the whole corpus turned up the same pattern, in wildly different phrasings ("per the relationship matrix", "Self-reference — this would be removed in actual implementation", "[No self-entry needed]", "[No hostile relationships listed in the relationship matrix for X]"), in **at least 10 other historical packages** spanning unrelated themes (corporate HR, political drama, fantasy, western, gothic, Disney-crossover, high-society gala). Confirmed `list_packages_with_meta_text_leak()`'s marker regex had zero coverage for any of this phrasing — a genuine, longstanding detector blind spot, not a fluke miss on this package.

**Decision (with Jonathan): patch this order now, extend the detector, defer a prompt-level fix pending real scale data.** Reasoning: the phrasing varies too much across hits to be one template bug — the model is freelancing a structural gap (character has no listed enemies) differently each time, so a proper prompt fix would need real design work and cross-theme testing, not a quick tweak. Better to get accurate frequency data from the extended detector first, then decide if a prompt fix is worth it.

**Fixed this package by hand** — rewrote both characters' `relationships` field (SQL `UPDATE` via `mcp__supabase__execute_sql`, using `#>> '{}'` to unwrap the jsonb string scalar, `replace()` on the exact matched substring, re-verified after) to drop the brackets and any meta phrasing, keeping the sentences in-voice and consistent with the rest of each character's writing. Re-selected both fields after the edit to confirm clean prose with no residual brackets.

**Extended `list_packages_with_meta_text_leak()`'s marker regex** (migration `meta_text_leak_detector_add_relationship_matrix_pattern`) to add `relationship matrix`, `established cast dynamics`, `self-reference`, `no self-entry needed`, `this would be removed in actual implementation`, and a general `\[no [^\]]{0,80}hostile` bracket-pattern catch. Re-ran the detector: this package no longer flags (confirming the fix), and the corpus-wide count is now **16 packages flagged (15 paid, 1 unpaid)** — real backlog, not yet remediated.

**Not determined — and not something to decide solo:** whether/when to remediate the other 15 paid packages now flagged by the extended detector, and whether the frequency once fully counted justifies the prompt-level fix discussed above. Both are follow-up decisions for Jonathan, not resolved by this addendum — this addendum only covers the current order plus the detector extension.

## Addendum 19 (2026-09-04): corpus check on Addendum 17's "accomplice trio" bug — 3 real paid hits, root-caused to a specific gap in `MM Live - Parent62`'s prompt, blueprint fix drafted (not deployed)

Follow-up to Addendum 17. Jonathan asked whether the "accomplice trio" bug had been addressed systemically. Ran the corpus check Addendum 17 deferred: every `mystery_packages` row joined to `mystery_characters` grouped by package, filtered to `count(*) filter (where character_role = 'accomplice') > 1`.

**4 packages corpus-wide, 3 paid.** One unpaid outlier ("Death In The Spotlight," 25 players, `is_paid=false`, generated 2026-04-21) has **9** `accomplice`-tagged characters — a different, older failure shape, deprioritized since it was never purchased or played. The 3 paid hits, checked individually:

| Package | Extra `accomplice`-tagged character | Actual narrative role |
|---|---|---|
| Lethal Mutations (Addendum 17, fixed) | Nikolai/Nikki Petrov | Genuine co-conspirator — planned the murder, served as lookout, confesses to it in `final_statement` |
| Sunset Songs Scandal (32p, purchased 2026-08-14) | Sienna Strobe | Innocent bystander who merely overheard a suspicious comment — no involvement in the crime |
| The Cognitive Dissonance Incident (16p, purchased 2026-07-27) | Dr. Jordan/Jules Blackwood | Institutionally complicit enabler (buried misconduct complaints) — `final_statement` explicitly states "I didn't kill Morgan," never claims to have helped plan or execute it |

**This split matters: it's one root cause producing two different symptoms, not one bug.** For Lethal Mutations, the reveal was actually wrong (a real conspirator went unarrested). For the other two, the reveal was already correct — Sienna and Jordan shouldn't be arrested, because they didn't do it. Confirmed both packages' `detective_script` reveals only ever named the murderer + the one formally-selected accomplice, matching this. The actual defect in those two is the `character_role` tag itself: `character_role === 'accomplice'` also drives `isReassignRole()` in `GuestDropoutPanel.tsx` (ADR-0088's Remove-a-Character feature) — a mistagged character would incorrectly trigger the heavyweight murderer/accomplice plot-reassignment flow if a host ever removed that guest, corrupting a package that was otherwise fine.

**Root-caused in the live blueprint (`temp-files/MM Live - Parent62 (Package ID Race Fix).blueprint.json`), read locally, nothing edited in Make.com.** Confirmed this repo's own code never writes `character_role` — it's set entirely by the Make.com parent scenario and this repo just persists whatever it sends (`grep -rln character_role` across `src/` and `supabase/functions/` turns up only downstream *readers*: `GuestDropoutPanel.tsx`, `adaptationService.ts`, `CharacterAccess.tsx`, none of them writers). Found the gap in Part 2's prompt: `motiveDistribution[]` (the per-character roster) assigns each character `"role": "[murderer/accomplice/redHerring/suspect]"` — free choice among 4 values per character, with **no constraint tying `accomplice` to the single character named in Part 1's `accompliceSelection`**, and Part 1's `<accomplice_handling>` block never explicitly forbids describing a second character as a co-conspirator. `has_accomplice` on the customer-facing form is a plain boolean (confirmed with Jonathan — the product has never offered multiple accomplices as an option), so any second `accomplice` tag is purely the model freelancing a richer conspiracy in Part 1's murderer-selection writeup and then carrying that framing into Part 2's roster — consistent across all 3 real hits, just landing with different narrative weight (full conspirator vs. mere enabler) depending on how far the model took it.

**Fixed the two live mistags directly** — `character_role` on Sienna Strobe (package `64d63a5d-041f-4814-aadb-25cf22acc4b7`) and Dr. Jordan/Jules Blackwood (package `a0a985a9-2d2d-4176-a825-82ee6b2990be`) changed from `accomplice` to `suspect` (SQL `UPDATE`, matches their actual narrative role, closes the Remove-a-Character mistrigger risk). No `detective_script` changes needed for either — those were already correct.

**Blueprint prompt fix drafted, deliberately NOT deployed.** Two text edits proposed for `MM Live - Parent62`'s Part 1 and Part 2 system prompts:
- Part 1 `<accomplice_handling>`: add "There is EXACTLY ONE accomplice, always. Do not develop any other character as a co-conspirator, lookout, or knowing participant in planning or executing the murder... A character can be written as morally complicit... WITHOUT making them a co-conspirator in the murder itself."
- Part 2 `motiveDistribution[].role`: constrain the field description so `'accomplice'` may only be assigned to the exact character named in `accompliceSelection.character`, with `'suspect'`/`'redHerring'` as the explicit fallback for complicity-flavored characters.

Not deployed because applying it means duplicating the live Make.com scenario into a new numbered version (this project's established never-edit-in-place convention — Parent49 through Parent62, each a new duplicate) and a real test generation to verify, which costs live Anthropic spend requiring its own explicit go-ahead. Jonathan reviewed and accepted the diff, so the file was built the same way every prior version bump has been (see `build-parent-v33.py` for precedent): `temp-files/build-parent-v63.py` applies both edits as verified single-match substring replacements to `Parent62`'s JSON-decoded prompt text and writes `temp-files/MM Live - Parent63 (Single Accomplice Constraint).blueprint.json`. Diffed leaf-by-leaf against Parent62 — exactly the 2 intended prompt fields changed, nothing else. Both files are local-only (`temp-files/*.py` and `temp-files/*.json` are gitignored, matching every prior Parent build script/export). No Make.com API call made, no scenario duplicated on the platform, no test generation run — importing `Parent63` into Make.com and testing it (real Anthropic spend) are still Jonathan's to do.

**Not done:** the unpaid 9-accomplice outlier ("Death In The Spotlight") wasn't investigated further — never purchased, so no customer impact, and 9 tagged characters suggests a distinct enough failure mode from today's 2-character pattern that it likely doesn't share the same root cause. No corpus-wide check for other `character_role` mistagging shapes unrelated to `accomplice` (e.g., a `redHerring` narratively behaving like a `suspect`) — out of scope for this addendum, which was specifically triggered by the accomplice-count query.

**Decision (2026-09-04, with Jonathan): validate v63 against the next real `has_accomplice=true` purchase instead of a synthetic test run.** Jonathan imported `Parent63` into Make.com and confirmed it's wired to the same production webhook `mystery-webhook-trigger` already calls — so it's now the live scenario, not just an imported-but-inactive duplicate — but explicitly chose not to run a paid test generation now. Follow-up for whoever runs the next `sweep` on an accomplice-enabled purchase: explicitly re-run the Addendum 19 corpus query (`character_role='accomplice'` count > 1) scoped to that package as part of the standard checklist, so the fix gets a real confirm/deny rather than being caught only if it happens to recur.

## Addendum 20 (2026-09-04): Addendum 18 follow-up — caught my own detector's false positives, root-caused the leak to a live (not dead) prompt concept, fixed 9 more paid packages, drafted a targeted prompt guard

Jonathan asked whether the 15 other packages the Addendum 18 detector extension flagged were recent enough to be worth fixing, and separately asked whether "relationship matrix" might be an unused artifact worth deleting. Both questions led to real corrections.

**Caught a false positive in my own regex before trusting the count.** The bare term `relationship matrix` (added in Addendum 18) also matches two legitimate, non-leak contexts: (1) the `rumors` field's standard host-instruction text — "target 3 DIFFERENT characters... one Hostile from your relationship matrix" — present in 3 older packages (Apr 2026), and (2) a literal in-story prop in one package's `detective_script` ("Jordan's personal notes and the relationship matrix," a forensic-lab theme). Confirmed via direct substring checks in both cases. Rescoped the migration (`meta_text_leak_detector_scope_matrix_pattern_to_narrative_fields`) so `relationship matrix`/`established cast dynamics`/`cast dynamics` only fire against `mystery_characters`' narrative fields (`introduction`, `background`, `secret`, `relationships`, `description`), not `rumors`/round-scripts/package-level fields — the other markers (`self-reference`, `no self-entry needed`, `this would be removed in actual implementation`, the bracket-hostile pattern) stayed global since they showed no false positives on inspection. Corpus count dropped from 16 to 11; of those, 2 (`Death At Villa Amore`, `Before The Nikah`, both Apr 2026) turned out to be a separate, pre-existing `[choose ` placeholder match unrelated to this addendum's pattern — left untouched, flagged as its own open backlog item, not conflated with today's fix.

**Answered whether "relationship matrix" is a dead artifact — it is not, and should not be removed.** It's Section 2 of the live master-context planning prompt (`temp-files/intrigue-master-constraints-prompts.md:112`, `2. RELATIONSHIP MATRIX — Map every character's relationship to every other character...`) — a load-bearing planning step, not clutter. Confirmed the current live Child blueprint (`MM Live - Child (Unified)36-WebhookLanguageField.blueprint.json`) already carries two anti-leak guards naming this exact phrase (`<no_chain_of_thought_in_output>` forbids "reviewing the relationship matrix"; `<no_meta_text_in_output>` forbids "Pick a character from your relationship matrix" and strips bracketed placeholders generally) — essentially the same language already in `regenerate-child-content/index.ts`. The guard exists; it just isn't 100% effective. Every one of the 9 confirmed leaks (below) shares one exact trigger the guard doesn't positively address: **a character with zero hostile relationships, or zero extra allies beyond one real entry** — the guard says what not to write for this case, never what to write instead, so the model falls back to narrating the data structure.

**Fixed the 9 remaining confirmed-genuine packages** (all paid, 2026-05-07 through 2026-08-30 — all within the last 4 months): "The Cookie, The Crown, And The Jester's Heart," "Murder In Paradise: Death At Coral Cove Resort," "Camp Blood Reunion: The Final Summer," "The Birthday Betrayal," "Murder On The Midnight Express," "Death At The Velvet Viper," "Death By High Tea: Murder At The Velvet Shaker," "Death At Sumpter Hall," "The Purchasing Director." Three distinct leak shapes required different fixes, all via SQL `UPDATE` + `#>> '{}'`/`replace()`, verified after each: (1) the empty-relationship fallback bracket (5 characters) — reworded to plain in-character prose or, where a redundant bracket trailed an already-real entry, deleted outright; (2) the older self-listed-as-own-ally bug (2 characters, same shape as the Aug 16 self-relationship bug already fixed in `regenerate-child-content`, e.g. `[Note: This is yourself; no self-entry needed]`) — deleted the nonsensical self-entry line entirely, leaving the real ally intact; (3) literal unfilled template placeholders (2 lines on 1 character, e.g. `[Hostile relationship needed] - [Additional rival from relationship matrix]`) — deleted, since there was no real content to preserve. Re-ran the detector after all 9: zero remaining hits from this pattern corpus-wide (the 2 unrelated `[choose ` hits noted above are the only survivors).

**Drafted a targeted prompt fix.** Duplicated the live Child blueprint to `MM Live - Child (Unified)37-EmptyRelationshipFallbackGuard.blueprint.json` (confirmed 36 was the current head before duplicating, per this ADR's own versioning convention) and added a new `<empty_relationship_case>` instruction to all 8 AI-call modules in the scenario: explicitly forbids the structural-absence phrasing, and — the part the existing guards were missing — tells the model what to write instead (one grounded in-character sentence, or nothing at all if there's truly no more to say). Validated the file is still well-formed JSON and diffed clean (exactly 8 targeted insertions, nothing else changed) against the source file.

**Status (2026-09-04): Jonathan imported v37 into Make.com.** Same caveat as Addendum 19's Parent63 import — this session has no Make.com API access, so it's confirmed the file was imported, not confirmed that the production webhook's active scenario is actually `Child (Unified)37` rather than some other version. Not yet validated against a real generation. Follow-up for the next `sweep` (or any `has_accomplice=false`/`=true` purchase with several minor/no-enemy characters): after this checklist's usual manual character spot-check, explicitly re-run `list_packages_with_meta_text_leak()` scoped to that package to get a real confirm/deny on whether v37 actually suppresses the empty-relationship leak, rather than waiting for it to resurface on its own.

**Closed the "2 unrelated `[choose ` hits" loose end instead of leaving it as vague backlog.** Read both: `Death At Villa Amore` and `Before The Nikah` both matched on `[choose a name or leave blank for host to personalise]` in the detective's opening statement — the exact same legitimate host-fill-in convention this checklist's step 5 already documents (analogous to `[MURDERER NAME]`-style brackets), not a leak at all. This was always a false positive in the *original* pre-Addendum-18 `\[choose ` marker (meant to catch authoring-instruction leaks like `[choose someone with a strong motive]`), just never previously surfaced because these 2 packages predate this detector or a prior sweep run. Confirmed via a corpus-wide check that no other package anywhere matches `\[choose ` with anything other than this exact host-fill-in phrase, so excluding it costs zero real detection coverage. Refined the marker to `\[choose (?!a name or leave blank)` (migration `meta_text_leak_detector_exclude_legitimate_choose_a_name_placeholder`). Re-ran the detector: **0 packages flagged corpus-wide** — the full backlog opened by Addendum 18/20, plus this pre-existing false positive, is now completely clear.

## Addendum 21 (2026-09-05): closed the "left as-is" gap from Addendum 2/3 — backfilled Cypress/Celine Beaumont's guilty branch, deliberately deferred for 11 days

A manual sweep (triggered by a request to investigate `package_completion_blocking_defects()` flagging `missing_round_content.Cypress/Celine Beaumont` on `b8428a57-1c2b-4bf1-881c-98c8436be6a9`, "Operation: Thirty & Murdery") found this wasn't a new incident — it's the exact gap Addendum 2/3 already root-caused and explicitly chose not to backfill on 2026-08-25: `round2_guilty`, `round3_guilty`, `round4_guilty`, and `final_guilty` (plus all four `_pointform` variants) were still `NULL` on this one character, out of 33 total, 11 days after the original incident.

**Why it resurfaced now.** The live `validate_package_characters()` trigger only fires on `UPDATE` to `mystery_packages` (ADR-0108), never on a direct edit to a `mystery_characters` row — so once this package's `generation_status` first read `'completed'`, nothing in the automated pipeline would ever re-check this specific character's fields again on its own. The gap was only ever going to be caught by a manual sweep calling `package_completion_blocking_defects()` directly, which is exactly what happened here.

**Confirmed the gap was still exactly as Addendum 3 left it** — `round2_innocent`/`round3_innocent`/`round4_innocent`, `round2_accomplice`/`round3_accomplice`/`round4_accomplice`, `final_innocent`/`final_accomplice`, and `reveal_confession_accomplice` all populated and correct; only the guilty branch (5 fields, matching Addendum 3's "one malformed call zeroes out everything it was responsible for" description) plus `reveal_confession_guilty` were empty. All 32 sibling characters have full guilty-branch content.

**Decided to backfill rather than re-defer**, since the detector is now actively flagging it on every manual sweep and the underlying `parse-claude-json` bug that caused the original failure was fixed the same day (Addendum 3) — a fresh regeneration attempt was very unlikely to repeat the exact same failure. Got explicit sign-off from Jonathan first (per his standing rule on paid API spend) with a ~$0.15–0.20 estimate before making any Anthropic call.

**Fixed:**
1. Confirmed the exact call contract via `regenerate-child-content`'s own documented POST body and `PromptGroup` definitions (`GROUP_ROUNDS_GUILTY` = `round2_guilty`/`round3_guilty`/`round4_guilty`/`final_guilty`, one call group, matching the Child blueprint's module-513 call boundary).
2. Ran a `dry_run: true` call first — confirmed the plan (1 target, 1 group, exactly the 4 requested fields, $0.10 estimate) before spending anything.
3. Ran the real call. Succeeded cleanly on the first attempt — all 4 fields generated, `outcome: "fixed"`, actual cost $0.10 (under the estimate, since only one call group was needed).
4. **Deliberately did not call `generate-pointform-summaries`** for the `_pointform` variants — same reasoning already on record in this ADR's main body (the "Deliberately NOT done" note on `reveal_confession_*_pointform`): that function has no field-level targeting, only a per-character `characterIds`/`characterName` filter, and would have re-summarized all ~20 already-correct pointform fields on this character (plus a second, unapproved Sonnet 5 call for `reveal_confession_*`) to backfill 4. Wrote the 4 `_pointform` fields by hand instead — zero additional API cost, matching the existing sibling-character bullet format (4-7 bullets, "- " prefix, ≤20 words each, no headers, no quoted dialogue) and grounded directly in the freshly-generated prose (the $310,000 unpaid commission motive, tour-guide method/opportunity, and the reveal confession).
5. Re-verified: `package_completion_blocking_defects()` now returns `NULL` for this package (was `["missing_round_content.Cypress/Celine Beaumont"]` before the fix). Manually re-checked all 8 fields (4 prose + 4 pointform) are non-null and read coherently against her established background/secret.

**Left open, matching the original scope decision:** `reveal_confession_guilty` (prose) and `reveal_confession_guilty_pointform` are still `NULL` — the fifth field from the same original Addendum 3 failure, but not checked by `package_completion_blocking_defects()`'s `missing_round_content` class (which only covers round2/3/4/final, not the reveal-confession beat) and outside the scope of what was approved for this spend. Per Addendum 2's original reasoning, this only matters if Recast/reassignment is ever used to make her the murderer — still true today, since `character_role` is unchanged (`suspect`). Flagged here rather than silently left for the next sweep to rediscover from scratch.

## Addendum 22 (2026-09-06): closed Addendum 21's remaining open item — `reveal_confession_guilty` backfilled, Cypress/Celine Beaumont now 100% complete

Follow-up requested the same way as Addendum 21: sign-off first, then execute. Got explicit approval for a second, separate ~$0.10 Anthropic spend (a distinct call group from Addendum 21's — `regenerate-child-content`'s `GROUP_REVEAL_CONFESSION`, not `GROUP_ROUNDS_GUILTY`) before touching anything.

**Fixed:**
1. Dry-ran `regenerate-child-content` with `fields: ["reveal_confession_guilty"]` first — confirmed the plan (1 target, group `reveal_confession`, exactly the 1 requested field, $0.10 estimate) and, critically, that requesting only `reveal_confession_guilty` does NOT pull in her already-correct `reveal_confession_accomplice` (the group's `fieldsToGenerate` is filtered to the intersection of the group's fields and the caller's requested fields — confirmed in code before relying on it, same mechanism verified in Addendum 21 for `GROUP_ROUNDS_GUILTY`).
2. Ran the real call — generated cleanly on the first attempt, actual cost $0.10. Confirmed via direct read afterward that `reveal_confession_accomplice` (1548 chars) was untouched.
3. Content reads coherently against her established guilty branch: same $310,000 unpaid-commission motive, the same tour-schedule opportunity, the same borrowed-vial/champagne-tray/orchid-scent method from `round2_guilty`/`round3_guilty`/`round4_guilty` — this is expected, since the prompt group's `anchorHints` show `final_guilty`/`final_accomplice` as coherence anchors when generating this field.
4. Hand-wrote `reveal_confession_guilty_pointform` (zero additional cost), matching the sibling-character format (7 bullets, confession-toned, grounded in the newly-generated prose) rather than calling `generate-pointform-summaries` — same reasoning as Addendum 21 point 4.
5. Re-verified: `package_completion_blocking_defects()` still returns `NULL`. Directly confirmed all 12 of this character's round/reveal fields (4 guilty prose + 4 guilty pointform + reveal_confession_guilty + pointform + reveal_confession_accomplice + pointform) are non-null. Cypress/Celine Beaumont is now the only one of the 33 characters in this package that's been individually field-by-field verified complete, rather than just clean-by-detector.

**Nothing left open on this character.** Both fields the original 2026-08-25 incident zeroed out are now fully backfilled across two separate, individually-approved spends ($0.20 total for this character's complete repair, plus the original $0.15 x 2 escalated-but-unapplied attempts on 2026-08-25).

## Addendum 23 (2026-09-06): corpus-wide check — the Cypress/Celine bug shape is confirmed a genuine one-off, zero other live instances

Follow-up requested to Addendum 21/22's own open item ("no corpus-wide check for other characters with the same single-character guilty-branch gap"). Ran a direct SQL query, not `package_completion_blocking_defects()`, scoped to the EXACT signature of this incident — a character where `round{N}_innocent IS NOT NULL AND round{N}_guilty IS NULL` (or the `final_` equivalent) — across every character in every package in the live database (169 packages, 87 `mystery_style='character'`):

```sql
-- per-character gap flags, then aggregate: packages with >=1 gapped character
-- AND >=1 character with both innocent+guilty populated (proves it's a
-- per-character anomaly, not a package-wide model mismatch)
```

**Result: zero rows.** No character, in any live package, currently has an innocent branch populated with a null guilty branch while any sibling character in the same package has both populated. Before this session's fix, Cypress/Celine Beaumont's row was exactly this shape (confirmed directly in Addendum 21/22's own investigation); now that it's repaired, this query returns nothing at all. **This confirms the 2026-08-25 `parse-claude-json` bug's damage was contained to the single incident already known and now fully repaired — not a wider silent-failure pattern.**

**Found something adjacent, deliberately NOT investigated further (Jonathan's call, scoped out this session):** the broader `package_completion_blocking_defects()` scan used to build the query above surfaced ~32 packages with SOME defect flagged (self_directed_question, meta_text_leak, identity_conflict, and a `missing_round_content` shape on several old packages). Spot-checked one — "Death On The Dance Floor" — and confirmed it's a *different* shape entirely: `round2_script` populated, `round2_innocent`/`round2_guilty`/`round2_questions` all null, i.e. an old single-script-model character mistagged/generated under `mystery_style='character'`. This exactly matches the historical corpus ADR-0096's own migration comment already named and sized (`Death On The Dance Floor` "17/18" characters affected, `Death At The Deadwood Saloon` "9/9" — both counts match this scan precisely) — a known, pre-existing, already-priced-in gap, not a new discovery from this session. **Left untriaged**, along with the rest of the ~32-package list — a real but separate investigation from what this addendum was scoped to answer.

## Addendum 24 (2026-09-07): sweep of "Murder In The Majlis" — clean on every automated/manual check except a stray trailing quote mark in 2 characters' guilty confessions, fixed

Routine New-Purchase sweep, conversation `e826a708-824c-49b6-858d-82f24106ef73`, package `a49181e0-a49f-499e-b491-98732799887d` (6-player, `mystery_style='character'` slip-style, no accomplice, generated 2026-09-06 — well post-ADR-0074 upgrade). All three scoped detectors (`list_packages_with_meta_text_leak`, `list_packages_with_victim_mismatch`, `list_packages_with_unresolved_victim_name`) returned zero rows, and `package_completion_blocking_defects()` returned null. Victim name ("Sheikha bint Hamdan Al Marri") cross-checked consistent across `game_overview`, `detective_script`, and all 6 characters' `background`/`relationships`/`secret` text. Full `detective_script` read clean — the only bracket present, `[The murderer (player) reads their confession aloud.]`, is the legitimate slip-style host-fill-in convention matching `game_overview`'s "the culprit is randomly selected at game time" design note. All 6 characters use the by-design dual-gender name convention (e.g. `Aisha/Aamir Al Mansoori`) per [[project_dual_gender_name_convention_sweep_note]] — not a leak.

Spot-checked all 6 characters' full content (not just 2-3, since the package is small) — `background`, `relationships`, `secret`, `rumors`, `reveal_confession_guilty`, `accusations` all coherent, no leaked authoring notes, no dangling references to removed characters (none were ever removed from this package). English-language, so the T-V register check (Addendum 13) doesn't apply.

**One real defect found and fixed:** 2 of 6 characters' (Hessa Al Nuaimi, Latifa Al Qassimi) `reveal_confession_guilty` prose ended with a stray, unpaired trailing single-quote character (e.g. `"...for her to spend.'"` with no opening quote anywhere in the field) — a generation artifact, since these fields are unquoted first-person monologue, unlike the `rumors`/`round*_questions` fields where every line is deliberately wrapped in matching quote marks (confirmed those were a false alarm on the same grep). The other 4 characters' `reveal_confession_guilty` and all 6 `_pointform` variants were clean. Fixed via a direct SQL `regexp_replace` stripping the trailing stray quote from just those 2 rows; re-ran all three detectors plus `package_completion_blocking_defects()` post-fix, still clean. Low severity (cosmetic, would only be noticed if this specific character drew the guilty slip and the host read the confession verbatim), but real — noting since a similar stray-trailing-punctuation shape hasn't been seen in a prior sweep and is worth having a name for if it recurs.

## Addendum 25 (2026-09-07): "Blood Moon Requiem" was clean content but stuck `needs_review` — traced to a month-old regression of ADR-0055's own fix, resolved

Sweep triggered by both a "New Purchase!" notification and a separate "Generation Display Issue" alert (later corroborated by a GitHub Actions health-check comment) for the same conversation `488f078b-349d-4066-9be8-18d3c50d03c9`, package `323e0cc7-801f-4d0e-b35c-69e36ebc4dfc` (14-player, `mystery_style='detective'`, no accomplice, generated 2026-09-07 03:35 — well post-ADR-0074). Content itself was clean: 14/14 characters, victim name ("Konstantin Draven") consistent across `game_overview`, `detective_script`, and 3 spot-checked characters' `background`/`secret`/`relationships`; `detective_script` brackets are all legitimate host-direction convention including a deliberate host tip pointing to real free tools (Google AI Studio / NotebookLM) for pre-recording audio — not a leak. Dual-gender name convention used throughout, correctly.

`generation_status.status = 'needs_review'` was real and correctly held: `package_completion_blocking_defects()` returned `self_directed_question.Sable/Saben Crimson` — a genuine chain-of-thought leak, not a false positive. Round 2's first question literally read `**To Sable/Saben Crimson (self-reflection aside; addressed instead to Ambrose/Ambrosia Sterling):**` — the model caught its own mistake mid-generation and left the correction as a printed annotation instead of just writing the corrected question.

This defect class has a known, free, deterministic auto-fix (ADR-0047/0055's `auto-remediate-packages` worker), but it hadn't fired despite ~3 hours elapsed. Root cause traced to a real, systemic regression — full account and fix in **[ADR-0055's Addendum](0055-widen-detector-rpcs-to-gate-held-packages.md#addendum-2026-09-07-the-fix-regressed-silently-for-a-month-found-via-a-routine-purchase-sweep-re-applied)**: a 2026-08-10 migration silently reverted ADR-0055's `needs_review`-visibility fix across all 8 detector RPCs it originally widened, live for a month before this sweep caught it. Fixed via a new migration re-applying the same widening (excluding the one detector created after ADR-0055 that was never part of its scope), then a scoped, zero-cost invocation of `auto-remediate-packages` (`{classes: ["self_directed_questions"], only_needs_review: true}`) to fix this customer's package immediately. Re-verified `package_completion_blocking_defects()` returns `NULL` and `generation_status.status` is now `completed`.

**Why this belongs in both ADRs:** the customer-facing symptom and its correct diagnosis (a real leaked chain-of-thought, not a false alarm) is a coherence-sweep finding: ADR-0103. The mechanism that stranded it — a shared status predicate silently drifting out of sync between the gate and the worker that heals it — is ADR-0055's problem domain, recurring for the second time in the same shape. Recording once each, cross-linked, rather than duplicating the technical account.

## Addendum 26 (2026-09-07): closed Addendum 24's open root cause — hardened `regenerate-child-content`'s JSON rules against the trailing-quote bug, not just the 2 rows

Addendum 24 fixed the 2 affected rows via direct SQL but explicitly left the generation-time cause untouched ("worth having a name for if it recurs"). Root cause: `CRITICAL_JSON_RULES`' rule 2 told the model to wrap "any quoted text inside string values" in single quotes — correct for `rumors`/`round*_questions`, whose schema template literally shows `'...'` around the text, but wrong for prose fields like `reveal_confession_guilty` and backgrounds/confessions, whose schema template shows plain unquoted monologue. The model generalized the instruction across both field types, occasionally leaving a lone trailing apostrophe on a field that was never supposed to be quoted at all — exactly the Hessa Al Nuaimi / Latifa Al Qassimi shape.

Reworded rule 2 to make the single-quote convention conditional on the field's own schema template rather than a blanket instruction, and extended rules 4/5's self-check pass to explicitly flag an unpaired trailing quote mark on an unquoted-prose field as a bug. Deployed to `supabase/functions/regenerate-child-content/index.ts`; confirmed live via `get_edge_function` (old blanket wording absent, new conditional wording present). Not independently re-verified against a fresh live generation yet — this closes the prompt-level root cause but the actual recurrence rate (zero vs. reduced vs. unchanged) is still an open question for the next sweep that happens to touch a package this pipeline regenerated.

## Addendum 27 (2026-09-07): sized the Addendum 24/26 trailing-quote bug across the full corpus, backfilled 28 confirmed rows, drafted the matching Make.com blueprint fix

Jonathan asked whether the Addendum 24 fix was systemic. It was — and a naive first pass badly overstated the scope, which is worth recording since the correction method is reusable. A "field ends with an apostrophe" scan across the 14 monologue-shaped fields (`round{2,3,4}_{guilty,innocent,accomplice}`, `final_{guilty,innocent,accomplice}`, `reveal_confession_{guilty,accomplice}`) flagged **3,853 hits across 48 packages** — almost all false positives. Many packages use a *different, valid* template convention where the entire field IS wrapped in matching quotes (e.g. `'I'll be honest with you—...'`), including a bulleted `- '...'` dialogue style my first structural check didn't parse, so it undercounted openings and overcounted "bugs."

Landed on two independent, cross-checked signals to isolate genuine cases: (a) a contraction-aware "real quote count is odd" parity check — strip only word-internal apostrophes (contractions like `don't`) before counting, which also flags true unpaired quotes while correctly excluding non-quote apostrophes like possessive plurals (`parents'` alone would otherwise false-positive); (b) a structural check that the field's actual prose content — after stripping markdown headers, bold labels, italic instructions, and bullet/numbered-list markers — does NOT itself open with a quote mark. Requiring **both** signals narrowed 3,853 raw hits down to **28 confirmed genuine cases** across 14 packages (13 `final_innocent`, 12 `reveal_confession_guilty`, 3 `reveal_confession_accomplice`). Manually read all 28 before touching anything, including one case (`Caleb`'s `reveal_confession_accomplice`) with a legitimate embedded mid-text quoted line *plus* a genuine stray trailing quote — confirmed the fix (strip only the final trailing quote character) correctly leaves the legitimate embedded quote pair untouched.

Backfilled all 28 via targeted SQL `regexp_replace` scoped by the exact confirmed-id set (not a blind global regex), one `UPDATE` per column. Re-ran `package_completion_blocking_defects()` across all 14 affected packages afterward — all clean. Re-ran the confirmed-case query once more post-fix: 0 remaining.

Drafted the equivalent prompt fix into the live Make.com child blueprint as `temp-files/MM Live - Child (Unified)38-StrayApostropheGuard.blueprint.json` — the blueprint's own `<critical_json_rules>` block (2 wording variants across 8 module occurrences: modules 501/505/509 detective-style, 513/517 character-style) carries the same root-cause bug Addendum 26 fixed in `regenerate-child-content`, including an even more pointed smoking gun in the short variant: *"Rumor ends with one closing apostrophe"* — rumor-specific phrasing copy-pasted verbatim into the guilty/accomplice/confession modules, where it doesn't apply. Same rewording pattern applied to both variants, verified the JSON still parses and all 8 occurrences updated identically via a Python script (not hand-edited, to guarantee exact consistency across occurrences). **Imported into Make.com by Jonathan 2026-09-07**, same day — not yet confirmed as the scenario the production webhook actually calls, not yet validated against a real generation (same open items as every prior blueprint import, e.g. Addendum 20).

**Deliberately left untriaged:** ~183 additional fields corpus-wide have an odd real-quote-count but do NOT fail the "opens with quote" check — some other quote-parity anomaly (a genuinely unpaired *opening* quote with no close, a nested-quote edge case, or a markdown pattern this pass's structural stripper still doesn't handle). A real but separate investigation from what this addendum was scoped to fix.

**Note on this addendum's own provenance:** this session found and root-caused the same bug independently, in parallel with the session that shipped Addendum 26 — both landed on effectively identical prompt wording for `regenerate-child-content` (confirmed via `git show`, not just recollection). Only one deploy took effect (both were byte-identical, so no conflict); this addendum's contribution is the corpus sizing, the 28-row backfill, and the blueprint draft, none of which Addendum 26 covered.

## Addendum 28 (2026-09-07): closed Addendum 27's "left untriaged" item — the remaining odd-quote-count fields are a false positive, not a second bug; scoped to packages from the last ~2 months per Jonathan's direction

Jonathan's guidance: only investigate mysteries from the last couple of months, not the full historical corpus — reasonable, since older delivered packages aren't worth re-opening at this point. Re-ran the "odd real-quote-count" scan scoped to `mystery_packages.created_at >= '2026-07-01'`: 39 fields across 17 packages, of which 1 was a trailing-quote case that turned out to structurally open with a matching quote too (false positive, correctly excluded by the Addendum 27 structural check — confirms the backfill is complete with zero regressions in this window). The remaining 38 all share one shape: no opening quote, no closing quote, plain unquoted prose throughout, yet an odd "real" apostrophe count.

Manually read 2 full fields to find the actual source of the parity imbalance — both times it was a legitimate **possessive plural** apostrophe my contraction-stripping regex doesn't catch (it only strips apostrophes flanked by letters on both sides, e.g. `don't`, `Jack's`; a plural possessive like `husbands'` or `folks'` has a letter before the apostrophe but a space/punctuation after, so it survives the strip and reads as a "real" quote character). Adeline Voss's `round2_guilty`: *"my three husbands' deaths"*. Georgie Finch's `round4_guilty`: *"other folks' furniture"*. Both fields are otherwise clean, coherent prose with zero actual quote-wrapping bug — the odd parity is fully explained by ordinary English grammar, not a defect. Given both spot checks (chosen from opposite ends of the flagged list, different packages, different characters) landed on the identical explanation, and all 38 share the exact same structural signature (no open, no close), this closes the item: **no second bug exists in the packages checked.**

This does not clear the full historical corpus (packages older than ~2026-07-01) of the same false-positive noise — that's fine, since it was never a real defect to begin with, just an artifact of the detection heuristic itself. No further action needed; the detection method (not the data) was imprecise, and it's now understood well enough not to re-chase.

## Addendum 29 (2026-09-08): "missing characters" complaint was mid-generation viewing, not a real gap — but the sweep found and fixed a genuine garbled reveal-line bug

**Trigger:** Nathan Dubois's purchase ("The Death Of Nathan's Twenties", `conversation_id` `d21deb52-2a63-4d6e-bbb7-6c3f14c32160`, `package_id` `5849f5a2-b7ef-4cb1-9cf4-b240a03e734a`, player_count=8, mystery_style='detective', has_accomplice=false, purchased 2026-09-07, post-Sonnet-5-upgrade) came with a support email: "I required 8 characters... only 5 were actually generated... I don't see an option to add the missing 3 myself."

**Character-count complaint: not a current bug.** `mystery_characters` has all 8 rows for this package, each with full background/introduction/round-script content (2000+ chars) and matching the 8 named characters in the conversation's `additional_details`. `extracted_characters` also lists all 8. `package_completion_blocking_defects()` returns null. However, the character rows were written in three bursts — 5 by 21:32, 2 more by 21:41, the last (Yvonne) at 21:51 — while `mystery_packages.generation_completed_at` was stamped `completed`/100% at **21:33:32**, ~17 minutes before the roster actually finished. `ready_email_sent_at` (21:52) correctly waited for all 8, so the email itself was fine, but the package page has no "generating character N of 8" indicator — a customer who loads the page in that mid-generation window (as Nathan evidently did) sees a normal-looking, non-"Finalizing" tab view with only however many characters have landed so far, no signal that more are coming. Confirmed this is not unique to Nathan's package: scanning the last 14 days of completed packages, 4 of 28 (14%) show `generation_completed_at` preceding the last character's `created_at` — one (`Operation: Thirty & Murdery`, 30 players) by **~3 hours**. This is a systemic premature-completion-timestamp pattern, not a one-off. **Not fixed under this addendum** — it's a UX/progress-indicator gap requiring a product decision (add a live progress indicator vs. accept async streaming latency), logged instead as a vault note for Jonathan's call. Separately confirmed via code search: there is no self-serve "add a character" flow anywhere in the app (ADR-0038 explicitly rejected this as a feature — cast size is fixed at generation) — moot here since all 8 already exist, but worth knowing before replying to any future version of this complaint.

**Real bug found and fixed: garbled detective_script reveal line.** The climactic reveal in `detective_script` read: *"...Fernande Cole's killer — I place you under arrest for the murder of Nathan Cole."* Two problems: Fernande (the murderer, Nathan's personal assistant) has no surname "Cole" anywhere else in the package, and grammatically the line implies Fernande *has* a killer — contradicting the fact that she's the one confessing in the preceding paragraph. Almost certainly the model conflated the parallel-construction pattern used for the victim ("Nathan Cole") with the murderer's name mid-sentence. This is read aloud by the host at the literal climax of the game, in front of all guests, so severity is high despite being a single line. Fixed via a targeted `UPDATE ... replace()` scoped to this exact `package_id`, matching the exact original substring — changed to *"Fernande — I place you under arrest for the murder of Nathan Cole."* Re-verified: substring no longer present, replacement string present, `package_completion_blocking_defects()` still null post-fix. `list_packages_with_meta_text_leak`, `list_packages_with_victim_mismatch`, `list_packages_with_unresolved_victim_name` all returned 0 corpus-wide for the current month (not a targeted signal for this specific bug, but a clean baseline). Manual cross-check of victim name ("Nathan"/"Nathan Cole") across `game_overview`, `detective_script`, and 4 characters' background/relationships (Fernande, Vanessa, Yvonne) found no other mismatch. Manual read of 2 characters' `accusations` field (Fernande, Mai — the two with unusually long accusations text vs. the other six's generic 189-char boilerplate) found deflection-note content consistent with the murderer/red-herring design, not a leak.

**Not logged as a CHANGELOG entry** — this was a one-package targeted SQL content fix (a garbled sentence in one customer's `detective_script`), not a code/pipeline change; it doesn't fit the "shipping behaviour" bar the CHANGELOG is for. The systemic premature-completion-timestamp finding is the durable artifact worth tracking going forward and is captured as a vault note instead (see below).

## Addendum 30 (2026-09-08): shipped the systemic fix for Addendum 29's "no progress indicator" finding

Addendum 29 flagged the gap but deliberately didn't fix it pending Jonathan's call on direction. He chose: add a progress indicator. Investigation found one already existed and was just being bypassed — `src/components/GenerationProgress.tsx` already renders a correct "N of M characters" screen with `charactersDone`/`charactersExpected`, wired up via `renderGenerationProgress()` in `MysteryView.tsx`. The actual bug was in the *reveal gate* deciding which screen to show: `MysteryView.tsx`'s condition for rendering the finished `MysteryPackageTabView` only checked `characters.length > 0` (at least one character), not whether the roster matched what was actually expected — so the moment `generation_status` flipped to `completed` (even prematurely, mid-roster) and `evidenceCards`/`detectiveScript` existed, it skipped straight to the full tab view with whatever partial roster had landed.

Fix: added `characterGateTarget` (expected character count, computed the same way as the existing progress bar, but capped at `player_count`) and `allExpectedCharactersPresent` (`characters.length >= characterGateTarget`), and used `allExpectedCharactersPresent` in place of the bare `characters.length > 0` check in both the reveal-gate condition and its complementary "fall back to progress screen" branch.

Two regression guards added after live-data testing surfaced real risk, not just theoretical:
1. **Capped at `player_count`.** `extracted_characters` has a known history of over-counting (ADR-0068/0069/0110/0120 — roster-parsing bugs). Querying currently-completed, paid packages found one live case (`eb0f3f1d-c404-49bb-8c7c-6daa135faa15`, "De Bittere Verjaardag"): `extracted_characters` says 18, but `player_count`=14 and `mystery_characters` actual count=14 — i.e., the package is genuinely complete and the extraction just over-counted. Without the cap, this (and at least 3 similar live packages found in the same query) would have been permanently stuck behind the progress screen post-fix, since `characters.length` can never reach an inflated target. `player_count` is the stable, customer-set ceiling a real roster should never exceed, so `characterGateTarget = min(extractedCount, player_count)`.
2. **4-hour freshness window (`generationRecentlyStarted`, gated on `generation_status.startedAt`).** Querying further turned up "Whispers From The Void" (`ffea7320-ba7e-48a3-a5c2-9fcf2ba8112a`) at 9 actual characters against `player_count`=10 — this is *intentional* (a Remove-a-Character adaptation, ADR-0036, confirmed by existing memory as a known-owned case, not a bug) and `player_count` never gets updated after a removal. Without a freshness gate, the stricter check would have permanently hidden this customer's own already-delivered, correctly-adapted package. The new check only applies while `generation_started_at` is within 4 hours (covers the worst observed real gap, ~3 hours on a 30-character package, with buffer) — older packages fall back to trusting `generation_status` alone, unchanged from prior behavior. Missing `startedAt` fails open (treated as not-recent), mirroring the existing `needsReviewWithinWindow` pattern's own null-handling philosophy in the same file.

Verified: `npx tsc --noEmit` and `npx eslint src/pages/MysteryView.tsx` both clean (zero new errors/warnings introduced — pre-existing `any`-type and hook-dependency warnings elsewhere in the file are untouched). Manually re-checked the fix's logic against real DB data for Nathan's package's actual historical timeline (5/8 characters at 21:33:32, `generation_started_at`=21:24:46 — well inside a 4-hour window — confirms the gate would have correctly shown the progress screen instead of the finished tabs) and against all 4 risky live edge cases found (Nathan's now-complete package, the extraction-inflation case, the Remove-a-Character case, and a 7-month-old 0-character "completed" package) — all resolve correctly with no regression, all now outside the freshness window by the time of testing. Did not attempt a live end-to-end browser reproduction inside the actual bug window (a genuinely-mid-generation paid package) — doing so would require either triggering a new real generation run (real Anthropic/Make.com spend, not authorized for a test) or waiting for a live purchase to land in exactly that ~20-40 minute window, neither of which fits this session.

Not touched: the underlying `generation_completed_at` timestamp staleness itself (Addendum 29's other finding) and the unidentified external writer that sets it prematurely — Jonathan's chosen fix addresses the customer-visible symptom directly and doesn't require resolving that upstream mystery. Left as-is per his decision (progress indicator only, not timestamp hardening).

## Addendum 31 (2026-09-08): two accomplice-branch content gaps found and fixed — plus closed the detection gap that let them ship unalerted, and a language-blind detector mistake caught mid-investigation

**Trigger:** sweep on a same-day purchase, "The Gods Must Be Gossiping: Murder At Aphrodite's Birthday Bash" (`conversation_id` `14d53082-df91-4eac-99f3-9c0c73e4ae1d`, `package_id` `ea610d24-c7c8-4c87-a842-59d64ffeabfb`, player_count=17, mystery_style='character', has_accomplice=false, EN, purchased 2026-09-08). All three named detectors and `package_completion_blocking_defects()` returned clean. Step 6's per-character spot-check found character **Pan/Pandora** missing `round2_accomplice`, `round3_accomplice`, `round4_accomplice`, `final_accomplice`, and `reveal_confession_accomplice` (prose AND pointform — 10 fields total, all null) while all 16 other characters had this content fully populated. The package's `detective_script` explicitly calls for an accomplice confession in the reveal ("I ask that person now, the one holding the accomplice slip, to step forward") — since role is drawn at the table in a slip-style game, any of the 17 players could have been dealt the accomplice slip and found the host had nothing to read them for 4 of the game's key beats. Fixed via `regenerate-child-content` (5 fields, $0.20) + `generate-pointform-summaries`; re-verified all 10 fields populated, content coherent (nice touch: Pan's confession references "down to my hooves," consistent with the character), zero detector regressions.

**Jonathan's question, mid-investigation: why wasn't I alerted — I only caught this because I happened to sweep.** Root-caused: `package_completion_blocking_defects()`'s `missing_round_content` check (ADR-0096) explicitly, deliberately excludes `round*_accomplice`/`final_accomplice` — the ADR's own text reasons this branch is "conditional on `conversations.has_accomplice`." That assumption is wrong for `mystery_style='character'` packages specifically: `validate_package_characters()` resets `conversations.has_accomplice` based on whether any `mystery_characters.character_role = 'accomplice'` is **pre-assigned** — which never happens in a slip-style game, since roles are drawn live at the table and never written to `character_role` at generation time. So `has_accomplice` for this style doesn't reflect whether the package's content includes an accomplice branch at all — it's simply not a reliable signal here, and today's package proves it: `has_accomplice=false` yet 16/17 characters correctly carry full accomplice content. `reveal_confession_guilty`/`reveal_confession_accomplice` were never checked at all, for any character, by this function.

**First attempt at a fix signal was itself wrong, language-blind — caught before shipping.** Initial hypothesis: gate the check on `detective_script ~* 'accomplice'` (does the package's own script establish an accomplice role exists). Verified this correctly distinguished packages with a legitimate accomplice role from murderer-only designs ("Murder In The Majlis" and 5 similar packages: `has_accomplice=false`, script never mentions "accomplice," and correctly have zero accomplice-branch content — not a defect). But running it against a Spanish-language package ("Veneno En La Medianoche," `package_id` `7c56a42d-a7ca-4a84-b7a0-0f507f32ea83`) proved the English keyword regex silently fails on non-English content — this package clearly has a real accomplice role (10/11 characters fully populated) but its script never contains the literal word "accomplice." This is exactly the English-pattern blind spot this ADR's step 6 already warns about for manual checks, now nearly baked into an automated one. Replaced with a language-independent **peer-existence signal**: if any *other* character in the same package has a given field populated, every character in a `character`-style package should (everyone might draw any role) — a real gap on a peer. A package with no accomplice role at all (nobody has the field) never trips this; no keyword-matching, no hardcoded rollout date needed for the newer `reveal_confession_*` fields either.

**Re-swept using the corrected peer-existence signal against all completed `character`-style packages.** Found exactly one more live instance, in the same Spanish-language package the language-blind attempt had exposed: **Aurelio Vanderbilt (Checho)** in "Veneno En La Medianoche," missing only `reveal_confession_accomplice` (round2/3/4/final_accomplice all present). Fixed the same way (1 field, $0.10 + pointform pass); verified the regenerated content came back correctly in Spanish ("Aurelio se pasa una mano por el rostro... 'No fui yo quien vertió el veneno'..."). Every other historical hit the sweep surfaced (~13 packages, 2025-08-14 through 2026-04-22, including Death On The Dance Floor and Death At The Deadwood Saloon by name) is the exact same pre-2026-08-11-fix backlog ADR-0096 already found and explicitly decided not to backfill ("a mystery party that old has almost certainly already been played") — confirmed via the peer-existence query, not re-litigated here.

**Shipped the detection fix**, via migration `add_missing_role_branch_content_defect`: extended `package_completion_blocking_defects()` with a new `missing_role_branch_content` defect class using the peer-existence gate, scoped to `mystery_style = 'character'`, covering `round2/3/4_accomplice`, `final_accomplice`, `reveal_confession_accomplice`, and `reveal_confession_guilty`. Verified read-only before deploying and again live after: correctly flags only the one real gap (Aurelio, pre-fix), correctly stays silent on Aphrodite (post-fix), Majlis (no accomplice role, by design), and the general case. Since `package_completion_blocking_defects()` is already wired into all three completion gates (ADR-0049's pattern), this required zero new wiring — same reuse principle ADR-0096 used originally.

**Explicitly scoped narrower than ADR-0096's full pattern, by choice:** ADR-0096 also widened `notify-generation-issue`'s auto-heal re-fire to match its new detection. Not done here — only 2 live instances found in the month since the last related fix landed (2026-08-11); wiring a new auto-heal path for a defect this rare is more standing machinery than the evidence currently supports. Alert-only (blocks completion status, fires the existing `notify-generation-issue` email) for now. Revisit if this recurs.

**Not done here:** root-causing *why* a single character out of many occasionally drops one field from an otherwise-successful batch response (the same "successful-but-incomplete" shape ADR-0096 flagged as unresolved for the original round-content class, and the same shape the `generate-pointform-summaries` code comment already documents for `reveal_confession_accomplice` on Haiku specifically — though this gap is in the Child *generation* call, not the pointform summarizer, and occurred at a much lower rate: 2 characters out of several hundred checked). This addendum closes the "ships undetected" gap, not the upstream generation-side cause — consistent with ADR-0096's own stated scope.

## Addendum 32 (2026-09-08): hardened `generation_completed_at` too — Jonathan opted for the cheap fix, skipped the Make.com blueprint hunt

After Addendum 30 shipped, discussed whether to also chase down the exact external writer responsible for the stale `generation_completed_at` (would require Make.com blueprint access this session doesn't have, and this ADR's own history of blueprint hunts — Addenda 19, 20, 27 — shows a pattern of real time spent for uncertain payoff, several still "not yet confirmed as the scenario the production webhook actually calls" days/weeks later). Before recommending, grepped every consumer of `generation_completed_at` across `src/`, `supabase/functions/`, `api/`: confirmed nothing reads its *value*, only null/not-null truthiness — so the stale timestamp has zero live customer-facing effect today. Recommended skipping the blueprint hunt but doing the cheap, self-contained trigger hardening (no blueprint needed, closes the gap regardless of writer, per ADR-0108's own stated philosophy). Jonathan agreed.

Fix: `validate_package_characters()`'s existing validated-clean branch (the `ELSE` of the `array_length(_reasons,1) > 0` check, which already correctly re-validates every transition into `completed`) now also stamps `NEW.generation_completed_at := now()`, overriding whatever any caller — this repo's `generation-complete.js`, the client self-heal in `mysteryPackageService.ts`, or Make.com's Child scenario writing directly — set on a premature attempt. Confirmed `promote_complete_packages()` (a plain `UPDATE`, no trigger bypass) is unaffected: its own precondition (`generation_completed_at IS NOT NULL`) is still satisfied by whatever the original premature writer set during the demoted interim state; this only refines the value at the moment genuine completion is actually confirmed.

Verified live (not just read the code): ran a `BEGIN`/`ROLLBACK`-wrapped transaction against Nathan's real package — forced `generation_completed_at` to a deliberately-stale `2020-01-01` alongside a fake `in_progress`→`completed` re-transition, confirmed the trigger overwrote it with a fresh timestamp (`2026-09-08 07:47:07`, matching test execution time), then confirmed the rollback left production data at its original (still historically-stale, unbackfilled) `21:33:32` value — no retroactive correction was attempted or warranted, this only prevents the bug going forward. Migration: `supabase/migrations/20260908074900_stamp_generation_completed_at_on_validated_completion.sql`.

## Addendum 33 (2026-09-08): health-check evidence-image alert was a self-resolved generation-order race, not a defect — but the sweep found and fixed a real inverted-motive bug in a character's secret

**Trigger:** a health-check GitHub Actions comment flagging "The Last Supper At Ravenscroft Manor" (`conversation_id` `b58b507b-f2e3-429e-bc5e-56b85f635ef7`, `package_id` `50817047-0bcf-46fe-9f21-55f701ccfd00`, player_count=6, mystery_style='character', has_accomplice=false, EN, purchased 2026-09-08) as a paid package missing evidence-card images, arriving alongside the purchase notification itself — same-day, post-Sonnet-5-upgrade.

**Evidence-image alert: not a live defect, a timing race.** `list_packages_missing_evidence_images()` (ADR-0016) flags a package when its `generation_status` is `completed`/`needs_review` but `evidence_card_images` is missing a round key the `evidence_cards` text references. Confirmed the GitHub comment posted at 16:42:53 UTC; all three evidence images (round2/3/4) landed in `storage.objects` between 16:43:07 and 16:43:30 — 14 to 37 seconds *after* the health check ran, with `generation_completed_at` not stamped until 16:54:00. The package's `generation_status` must have read `completed`/`needs_review` in that ~15-second window before the images finished uploading, which is the same premature-status shape as Addendum 29/30's reveal-gate bug and Addendum 32's `generation_completed_at` race, just hitting a different consumer (the health check's own query, not customer-facing UI). Re-ran the detector live post-sweep: package no longer appears, all three images confirmed present and correctly sized (1.0–1.4MB each) in storage. No fix needed here — Addendum 30's `MysteryView.tsx` reveal-gate fix already protects the customer-facing symptom of this same race; the health check catching a mid-flight state and self-clearing on the next run is expected behavior for a scheduled poll, not a new gap.

**Real bug found: Winston/Winnie Hargrove's `secret`/`secrets` fields had the pension theft backwards.** Every other piece of this character's content — `description`, `background`, all four `round*_innocent`/`round*_guilty` branches, both `final_innocent`/`final_guilty`, `reveal_confession_guilty`, the rumor another character spreads about them, and the plot's own `master_context` (`howVictimWrongedThem`, `specificMotive`) — agree Reginald diverted ~£9,000 from Winston's pension and threatened them into silence when they objected. Only `secret` and `secrets` (the two duplicate-format columns, same shape as the round-script dual-format gap in [[reference_round_script_formats]]) told the opposite story: that *Winston* had embezzled the £9,000 to cover a private debt and preemptively raised it with Reginald to deflect suspicion, which Reginald "saw through." A player reading this character's own materials would get two irreconcilable accounts of who stole from whom — high severity, since this is exactly the kind of self-contradiction step 6 of this checklist exists to catch, and it lands on the motive text for a character who might be dealt the murderer slip.

No `master_context` field defines a second hidden fact for Winston beyond the pension grievance, so rather than invent a new plot beat, rewrote `secret`/`secrets` to correct the misattribution while adding a self-contained detail consistent with everything else already established: Winston secretly drafted (but never sent) resignation letters to two other households in the days after the threat, evidence their anger had moved from muttering toward a concrete plan to leave — a genuine hidden fact distinct from the already-public grievance, structured the same "why this matters if exposed" way as the other five characters' secrets. Fixed via targeted `UPDATE` scoped to this `package_id` + character name only; verified both columns now match and read consistently with the rest of the package. All three named detectors and `package_completion_blocking_defects()` stayed clean before and after. Not logged as a CHANGELOG entry, matching Addendum 29's precedent — a one-package, one-character targeted content fix, not a code/pipeline change.

## Addendum 34 (2026-09-08): root-caused Addendum 33's secret-inversion bug to a real gap in the live prompt — fix drafted, not yet imported

Jonathan asked how often this had occurred and how likely it was to recur before agreeing to spend on an LLM-based detector. Answered both with evidence instead of a guess.

**Frequency check (free, no API spend):** pulled 15 other characters across 8 other packages whose `background` or `secret` matched a theft/embezzlement keyword regex (`diverted|embezzl|stole|stolen|siphon|skimm|forg|padded|misappropriat|took...money`) and read each by hand. Of those, 3 had the fact stated in *both* fields where a reversal was actually checkable (Persephone, Father Cynric/Sister Cynwyn, Anna) — all three internally consistent, no reversal. Zero additional instances found in this sample. Small and non-random (keyword-filtered, not a full corpus read), but real evidence rather than a guess, and cheap enough to do before spending anything.

**Root cause found: this is a real, live gap, not a random one-off.** Checked the `<content_coherence_rules>` block in `temp-files/MM Live - Child (Unified)38-StrayApostropheGuard.blueprint.json` — the actual production Child blueprint that generated Winston's package (confirmed same schema also mirrored in `supabase/functions/regenerate-child-content/index.ts`'s `GROUP_IDENTITY`). The `background` field's schema template explicitly instructs "*pull specific details from master_context*"; the `secret` field's template has no equivalent grounding instruction — nothing tells the model to check the secret against the background it just wrote two fields earlier in the same completion. `description`, `background`, `relationships`, `secret`, and `introduction` are all generated together in one call (confirmed via the same schema block appearing 8 times, once per character-slot module, matching the pattern already established in Addendum 27) — so this is a within-completion reasoning gap, not two independent calls drifting apart. Given the gap is real and specifically likely to bite on financial/theft-shaped motives (a common trope the model can reach for independently of what it already wrote), called this "plausible, not rare" going forward until closed, despite the clean sample.

**Recommended a one-line prompt fix over an ongoing LLM-based detector** — fixes the cause for free instead of paying to catch the symptom after the fact (~$5-10/month for a semantic-contradiction checker, per the cost breakdown discussed but not commissioned). Jonathan agreed.

**Fix drafted:** `temp-files/MM Live - Child (Unified)39-SecretMotiveConsistencyGuard.blueprint.json`, built off v38 with a single new bullet inserted into `<content_coherence_rules>` (added to all 8 per-character modules, same pattern as Addendum 20/27's blueprint fixes): *"SECRET vs. ESTABLISHED MOTIVE: If this character's background already establishes a specific wrong the victim did TO them ... the secret field must NOT invert that direction ... Before writing the secret, re-read the background paragraph you just wrote above and confirm who-did-what-to-whom stays the same."* Verified via script: exactly 8 occurrences of the anchor text found and replaced, resulting file still valid JSON, same module count (8 `anthropic-claude:createAMessage` calls) and same top-level structure as v38 — nothing else touched. **Imported into Make.com by Jonathan 2026-09-08** — not yet confirmed as the scenario the production webhook actually calls, not yet validated against a real generation (same open items as every prior blueprint import in this ADR). Does not retroactively fix the 167 existing packages; only prevents new ones from getting this specific shape of bug.

## Addendum 35 (2026-09-09): `missing_role_branch_content` caught a fresh live instance exactly as designed — two characters, `regenerate-child-content` fix run with explicit spend approval

**Trigger:** sweep on a same-day purchase carrying a `needs_review` alert, "Blood Tide: The Cove Of Broken Oaths" (`conversation_id` `874f4f59-3a3b-4bce-93df-80a8a882fcc5`, `package_id` `f40984b1-8b95-4df0-b589-5e75b81e4127`, player_count=18, mystery_style='character', has_accomplice=true, EN, purchased 2026-09-09). All three named detectors (meta-text leak, victim mismatch, unresolved victim name) came back clean. `package_completion_blocking_defects()` — the function Addendum 31 built specifically for this defect class — flagged `missing_role_branch_content` for exactly 2 of 18 characters: **The Beachcomber, Wick/Wanda Orlov** (entire accomplice branch null: `round2/3/4_accomplice`, `final_accomplice`, `reveal_confession_accomplice`, all 5 fields) and **Netmaker Tomas/Talia Finn** (only `final_accomplice` null, rest of that character's accomplice branch present). All other 16 characters had complete accomplice content — confirmed by hand querying null-ness of all 5 accomplice-branch fields across every character in the package, not just trusting the defect list.

**Fix, with an explicit spend gate this time.** Unlike Addendum 31 (same-session judgment call), this fix required a live Anthropic API call, so — per the global no-paid-API-without-permission rule — ran `regenerate-child-content` in `dry_run` mode first (free) to get an exact plan (2 Claude calls / $0.20 for Beachcomber's 5 fields across the `rounds_accomplice` + `reveal_confession` prompt groups; 1 Claude call / $0.10 for Netmaker's single field), presented it to Jonathan, and got explicit go-ahead before spending. Both live calls returned `"outcome":"fixed"` with all requested fields `"status":"generated"` — actual spend $0.30, matching the dry-run estimate exactly.

**Re-verification, not just trusting the tool's own success response:** re-ran `package_completion_blocking_defects()` (returned `null` — no defects) and all three named detectors (all zero rows) post-fix. Manually read both characters' full regenerated text: no leaked brackets or authoring notes, victim name ("Captain Odessa Vane") consistent with `game_overview`, no cross-character contamination, tone/register consistent with each character's existing voice (Beachcomber's accomplice branch keeps the scavenger/tide imagery established in their other fields; Netmaker's ties back to the debt-to-Vane motive already established elsewhere in that character). `validate_package_characters()`'s trigger (the same one from Addendum 32) picked up the field writes automatically and flipped `generation_status` back to `completed` with `needs_review_at` cleared — no manual status update needed, confirming the self-heal path Addendum 31 relies on works end-to-end on a genuinely different package.

**Nothing new shipped here** — this addendum is a clean confirmation that Addendum 31's detector and fix mechanism work as designed on an unrelated live purchase, not a new bug or gap. Recorded per this ADR's own logging convention (every sweep hit gets an addendum, clean sweeps don't) and because a CHANGELOG entry was warranted (real content fix, unlike the single-character content edit in Addendum 33 which stayed ADR-only).

## Addendum 36 (2026-09-09): wired `missing_role_branch_content` into the auto-heal pipeline — the "revisit if this recurs" bar from Addendum 31 was met

Jonathan asked, after Addendum 35's manual fix: "is there anything we need to put in to make sure this doesn't happen in the future or is it just the way it is?" Addendum 31 (2026-09-08) had already drawn this exact line: it built detection for this defect class but deliberately left the fix alert-only, reasoning "only 2 live instances found... wiring a new auto-heal path for a defect this rare is more standing machinery than the evidence currently supports. Revisit if this recurs." Addendum 35 is that recurrence — a second, unrelated package, one day later. Answered with a build, not a shrug: the generation-side root cause (why a character batch call occasionally drops one field) stays a known, accepted, unchased rarity per Addendum 31's original call; the self-heal gap was the part actually worth closing, and it was already flagged as a deferred decision, not a settled one.

**What shipped:** `auto-remediate-packages` (ADR-0047/0061) already self-heals three other character-scope defect classes (`identity_contamination`, `slip_culprit_leak`, `meta_text_leak`) by delegating to `regenerate-child-content`, each dispatched from its own `list_packages_with_*` RPC. `missing_role_branch_content` had no such RPC — only `package_completion_blocking_defects()`'s flat `text[]` of `"class.character"` strings, built for the completion-gate triggers, not for a delegation loop that needs a precise per-character field list. Added `list_packages_with_missing_role_branch_content()` (migration `20260909180000_add_missing_role_branch_content_list_detector.sql`), the missing counterpart: one row per (package, character) naming exactly which of the 6 accomplice/reveal-confession fields are missing on that peer, using the identical peer-existence gate as `package_completion_blocking_defects()`. Verified byte-for-byte equivalence before relying on it: a query diffing every `package_id`+`character_name` hit between the new function and `package_completion_blocking_defects()` across the last 45 days returned zero rows in both directions.

Wired it as the 4th delegated class in `auto-remediate-packages`: new `DefectClass` entry, `DETECTOR_RPC` mapping, and `handleMissingRoleBranchContent()` — simpler than the other three's handlers because the detector row already carries the exact missing-field list for that one character (no `DELEGATE_DEFAULT_FIELDS` seed, no auto-widening needed). `regenerate-child-content`'s `DefectHint` type gained the new literal purely for correct `auto_remediation_log` labeling — `autoExpand`/`effectiveFieldsFor` both already no-op for any hint they don't explicitly branch on, which is exactly the right behavior here (the caller already computed the precise fields; no widening should happen).

Added the new class to the 5-minute held-only sweep's `classes` filter (`cron.alter_job` on job id 9, `auto-remediate-held-packages`) alongside the other three delegated classes, so a future instance self-heals within ~5 minutes of hitting `needs_review` instead of waiting for the 30-minute full sweep or a human running `sweep` manually. The 30-day `WINDOW_DAYS` bound (unchanged, applies to every class) keeps the ~13 pre-2026-08-11 historical instances Addendum 31 explicitly decided not to backfill out of scope by construction, not a special-cased exclusion.

**Verified live, not just read the code:** deployed both edge functions (`regenerate-child-content` v16, `auto-remediate-packages` v11 — confirmed `verify_jwt: true` preserved on both, per [[feedback_git_push_not_deployed]]'s standing lesson that a deploy must be checked, not assumed). Ran `auto-remediate-packages` in `dry_run` mode three ways — filtered to just the new class, unfiltered across all classes, and with the exact `only_needs_review` + `classes` payload the cron now sends — all three completed cleanly with `considered: 0` (correct: Addendum 35's fix already cleared the only live instance, and the new list function independently confirms zero packages currently need this repair).

**Not done here, deliberately:** no change to the generation-side root cause (still an accepted rarity per Addendum 31); no backfill of the historical pre-2026-08-11 packages (same reasoning as Addendum 31 — already played); no LLM-based "why did this field drop" detector (never proposed, not the gap that was open).

## Addendum 37 (2026-09-10): a truncated LLM reply propagated a broken character name into a live paid package — fixed; roster-count mismatch alert traced to the same truncation, left open as a scope question for Jonathan

**Trigger:** sweep on 3 same-day purchases (Terminus 13, `787bfbba-547a-4a44-a555-0b98b4ba525b`/`e86e0bee-560e-435b-910a-5e636e0421bc`; The Last Caravan Of Riften, `84e09db1-9cd6-4a15-96cd-d1a2ce174363`/`a286b582-b5fc-4d57-a6ef-76e7b006e3f5`; Murder At The Rhinestone Rodeo, `778bb0d8-e340-441b-a9f1-d1427c4545ba`/`8198170c-8e0d-4f41-88b9-06da7553ead0` — all EN, `mystery_style='detective'`, all post-ADR-0074 Sonnet 5), paired with a same-day GitHub health-check alert (🔴 roster-count mismatch, Terminus 13, `approved=14, actual=15`).

**Investigation.** All three named detectors (meta-text leak, victim mismatch, unresolved victim name) and the broader `list_packages_with_*` suite (evidence-culprit spoiler, final-statement confession leak, identity conflicts, missing-role-branch content, self-directed questions, slip-culprit leak, unconfessed culprit, victim-as-character, characters-absent-from-conversation) all came back clean on all three packages, as did `package_completion_blocking_defects()`. Manual victim-name cross-check (every character's `background` against the victim's name) passed on all three (15/15, 10/10, 8/8). Manual secret-vs-background read of murderer/accomplice/red-herring characters across all three found no inversions (the Addendum 33 bug class) and no leaked authoring notes.

The roster-count alert led somewhere real, though. Reproduced with the shipped `scripts/detect-roster-mismatches.mjs` (not a hand re-derivation): Terminus 13's `approved_concept_message_id` message — the customer's own concept-approval reply, sent 2026-09-09 22:31, 5 minutes before purchase — is a numbered 28-character roster that the model's own reply cuts off mid-word at character #15: `"15. **Scavenger Row/R"`, no continuation message follows it in the conversation (it's the last assistant turn). `extractRosterFromMessage` correctly parses only the 14 complete entries before the truncation, hence `approved=14`. The delivered package has 15 `mystery_characters` rows — the 14 complete ones plus a 15th generated **from the truncated line itself**: `character_name = 'Scavenger Row/R'`, a broken half-written dual-name with no surname, matching the exact truncation point. The generated `background`/`introduction`/`description` for that character are otherwise complete, well-written, and internally consistent (using "Row" throughout as the working name) — only the `character_name` field and one `**Name:**` header line embedded in `background` carried the truncated string forward. Checked for wider blast radius (other characters' `relationships`/`background`, `game_overview`, `detective_script`, `host_guide`, `evidence_cards`, `materials`, `quick_reference`) — zero other references to the broken name; fully contained to one row, two fields.

**Fixed** (plain SQL text update, no Anthropic call, no permission needed): `character_name` → `Scavenger Row/Robin Teague`, and the matching `**Name:**` line inside `background`. Re-verified: `character_name` and header both read the completed name; no other field references the old truncated string.

**Left open, not decided unilaterally — the 28-vs-15 scope question.** The customer explicitly asked for 28 players three separate times across the conversation (initial brief: "for 28 players with full scripts"; a later message: "we're building for 28 scripted characters"; the truncated approval message's own header: "Character List (28 players)"). `conversations.player_count` reads 15 and the package was generated to match 15, not 28 — consistent with the documented, still-open [[project_stale_snapshot_never_recaptures]] gap (`player_count` is a form-captured value that chat-only scope changes don't update), compounded here by the reply truncation cutting the concept short at almost exactly the stale count. Whether the customer actually wants a 15-character game (deliberately scaled down at checkout, unrecorded in this conversation) or paid for and is expecting the 28-character world they spent two sessions designing is not something the data resolves — and completing it would mean generating ~13 more characters, a real Anthropic spend requiring explicit go-ahead per the no-paid-API-without-permission rule, plus likely contacting the customer to confirm intent. Reported to Jonathan as a decision point rather than assumed either way.

**Not done:** did not acknowledge the roster-count alert via `acknowledged_health_alerts` — it will keep firing on Terminus 13 until the scope question above is resolved one way or the other, which is the correct behavior for an escalate-only detector while the underlying question is still open.

## Addendum 38 (2026-09-10): replaced the blanket "wait for a 2nd occurrence" policy with an impact/cost framework, fixed the Addendum 37 bug at its source, built the backstop detector, and audited a month of deferred decisions against the new framework

Addendum 37 found a severe bug (13 of 28 promised characters silently missing, one delivered with a broken name) on its FIRST live occurrence — the exact situation this ADR's own established convention (every prior addendum: register mismatch, secret/background inversion, `missing_role_branch_content` before Addendum 35/36) says to leave alert-only until a second instance confirms the pattern. Jonathan asked directly whether that convention is actually best practice, given he runs the daily sweep that's been the real safety net and is sometimes away 5-10+ days at a stretch.

**The refined policy.** "Wait for recurrence" is correct specifically when detecting a bug requires fuzzy/semantic judgment (an LLM call per package, real ongoing spend under the no-paid-API-without-permission rule) AND the impact of missing one instance is subtle/bounded (a host might not even notice). Neither held for Addendum 37: the bug was cheaply, deterministically detectable (the concept message's own header states a total; the parsed roster falls far short — no LLM needed), and the impact was severe and obvious (46% of a paid order silently missing). Under the new framework: build immediately when detection is cheap+deterministic and impact is severe+obvious, regardless of occurrence count. Keep deferring when detection is fuzzy/expensive and/or impact is subtle. This doesn't retroactively fault the prior "wait" calls — the register/secret-inversion/branch-content bugs all genuinely need semantic judgment — it narrows when "wait" applies rather than treating it as a default for every new bug class.

**Root cause fixed at the source, not just detected downstream.** `mystery-ai/index.ts` was calling Anthropic with `max_tokens: 2000` — a value never deliberately tuned for this prompt, just carried forward unexamined through every `claude-3.5-sonnet` → `claude-sonnet-5` model swap since the feature's original integration (confirmed via `git log -L`). Terminus 13's concept reply hit that ceiling and got cut off mid-word; `stop_reason` (`'max_tokens'` when truncated) was available in every Anthropic response this whole time but never read. Fixed: raised the cap to 4000 (costs nothing unless a reply actually needs the tokens — it's a ceiling, not a target), and added one bounded continuation call whenever `stop_reason === 'max_tokens'` — the partial reply plus a "continue exactly where you left off" turn, concatenated, capped at one retry to bound cost. This fixes the bug before a truncated reply is ever shown to a customer or approved, not after.

**Backstop detector for messages already in the database** (`scripts/detect-truncated-concept-messages.mjs`, wired as health-check.yml check 15, ESCALATE-ONLY like check 12): compares a concept message's own stated header count ("Character List (28 players)") against both the parseable roster AND the actual delivered `mystery_characters` count, using a new `extractStatedRosterCount()` added next to the shipped `extractRosterFromMessage()` in `mystery-webhook-trigger/index.ts` (same "shipped code, not a hand-copy" discipline as the roster-mismatch script, ADR-0057). Building this immediately (not waiting for a second live instance) is itself an application of the new framework — the check is pure structural comparison, no LLM, and the failure mode is exactly Addendum 37's.

Tested against full package history before wiring into CI, not just the one known case: an early version flagged a second package, "Death At The Velvet Viper" (stated 60, parsed 42) — but that package's ACTUAL delivered count is 30, correctly matching `player_count`, because its approved-snapshot pointer is a stale abandoned draft (a known, separate ADR-0069 gap) that production never actually generated from (its parsed count of 42 exceeds the 35-character cap `detect-roster-mismatches.mjs` already documents production as requiring before trusting a parse — outside that bound, generation falls through to a different path entirely). Two refinements fixed it: requiring the ACTUAL delivered count to also fall meaningfully short (not just the message's own stated-vs-parsed numbers, which can both be stale together), and reusing the same 35-character trust bound. Re-ran across all history after both fixes: one hit, Terminus 13, zero false positives.

**Audited the last month for other items sitting under the old "wait" default that the new framework would actually call for building now** (delegated to a background research agent across `CHANGELOG.md`, this ADR's addenda, `monitoring/health-status.md`/`.github/workflows/health-check.yml`, and memory files; findings verified against current DB/code state, not trusted at face value). Four Tier-1 ("build now") candidates surfaced beyond today's fix: (1) `approved_concept_message_id` never re-capturing after a later roster revision (ADR-0069) — directly adjacent to this incident, one of three previously-drafted candidate fixes is a deterministic structural check, not yet built; (2) the ~19-20 of 30 `has_accomplice`-mismatch packages never individually swept for the same bracket-leak root cause already confirmed and fixed in 10 others (ADR-0120); (3) a second prior near-miss on this exact truncation shape from 2026-08-25 (a different customer's roster split across chat messages) that was explicitly flagged "worth a follow-up if a customer's roster comes back incomplete rather than just unparseable" and never followed up — today's incident IS that follow-up; (4) a leaked `Ihre/Deine` template placeholder found inside the German register-mismatch investigation (Addendum 13) — cheap string-leak detection, distinct from the genuinely fuzzy full-register check it was found alongside. None of these four have been built yet as of this addendum — reported to Jonathan as a punch list, not actioned here, since each needs its own scoping conversation rather than being bundled into this one.

**Not done:** no change to `approved_concept_message_id`'s re-capture logic itself (Tier-1 item #1 above — real, separate scope); no backfill sweep of the ~19-20 unswept `has_accomplice` packages (item #2 — real, separate scope); no hard generation-time gate that blocks/holds a package before completion when a concept message looks truncated (considered and rejected in favor of the escalate-only detector — a hard gate carries its own false-positive risk on a revenue-critical path, and with the root cause now fixed at the source, the backstop's residual job is narrow enough that escalate-only, matching how the sibling roster-mismatch check already works, is the proportionate response).

## Key files

- `CLAUDE.md` — the `sweep` shorthand command definition
- `supabase/functions/adapt-mystery-apply/index.ts` — `nameVariants()`, `dropBlocksTargeting()`
- `sweep_incomplete_packages()` (DB function) — the 2-minute stuck-package sweep, now with a quiet-period gate (Addendum 14)
- `supabase/functions/notify-generation-issue/index.ts` — now has its own quiet-period gate, independent of caller (Addendum 15)
- `supabase/functions/regenerate-child-content/index.ts` — now supports `reveal_confession_guilty`/`reveal_confession_accomplice` (Addendum 15); `_pointform` variants still unsupported, tracked as an open follow-up. `CRITICAL_JSON_RULES`' quote-closing rule is now conditional on each field's own schema template instead of blanket-applied (Addendum 26)
- `src/utils/titleExtraction.ts` — `extractTitleWithConfidence()` now exposes match-pass confidence; `extractTitleFromMessages()` is a string-only wrapper around it (Addendum 16)
- `src/pages/MysteryChat.tsx` — per-message title write now lets a `# TITLE` heading match overwrite a non-placeholder title (Addendum 16); `src/pages/MysteryView.tsx`'s separate title-write effect still has its own stale predicate, not fixed under this addendum
- `src/components/RecentSalesPopup.tsx` / `src/hooks/useRecentSalesPopup.ts` — homepage widget that displays `conversations.title` raw via `get_recent_public_sales` RPC, zero validation (Addendum 16)
- `list_packages_with_meta_text_leak()` (DB function) — marker regex extended to catch "relationship matrix" / "cast dynamics" / "self-reference" style leaks (Addendum 18), then rescoped after a false-positive sweep, all confirmed real hits fixed, and a second pre-existing false positive (`[choose a name...` host-fill-in convention) excluded (Addendum 20) — 0 packages flagged corpus-wide as of 2026-09-04
- `temp-files/MM Live - Parent62 (Package ID Race Fix).blueprint.json` — live parent generation blueprint (last deployed version). `temp-files/build-parent-v63.py` applies the multi-accomplice-mistagging fix (Addendum 19) and writes `temp-files/MM Live - Parent63 (Single Accomplice Constraint).blueprint.json` — built and verified locally, not yet imported into Make.com or tested
- `temp-files/MM Live - Child (Unified)36-WebhookLanguageField.blueprint.json` — previously live child generation blueprint, already carried anti-leak guards for "relationship matrix" phrasing but not the empty-relationship edge case. `temp-files/MM Live - Child (Unified)37-EmptyRelationshipFallbackGuard.blueprint.json` adds that positive instruction to all 8 AI-call modules (Addendum 20) — imported into Make.com by Jonathan 2026-09-04; not yet confirmed as the scenario the production webhook actually calls, not yet validated against a real generation. `temp-files/MM Live - Child (Unified)38-StrayApostropheGuard.blueprint.json` applies the Addendum 26 trailing-quote fix to all 8 modules' `<critical_json_rules>` blocks (Addendum 27) — imported into Make.com by Jonathan 2026-09-07; not yet confirmed as the scenario the production webhook actually calls, not yet validated against a real generation. `temp-files/MM Live - Child (Unified)39-SecretMotiveConsistencyGuard.blueprint.json` adds the Addendum 33/34 secret-vs-background consistency rule to all 8 modules' `<content_coherence_rules>` blocks (Addendum 34) — imported into Make.com by Jonathan 2026-09-08; not yet confirmed as the scenario the production webhook actually calls, not yet validated against a real generation
- `package_completion_blocking_defects()` (DB function) — new `missing_role_branch_content` defect class, peer-existence gated, `mystery_style='character'` only (Addendum 31); see also [ADR-0096 Addendum](0096-missing-round-content-blocking-defect.md)
- `list_packages_with_missing_role_branch_content()` (DB function, new) — the `list_packages_with_*` counterpart to `package_completion_blocking_defects()`'s peer-existence check, built for `auto-remediate-packages`'s delegation loop; returns per-character missing-field lists, not a flat string array (Addendum 36)
- `supabase/functions/auto-remediate-packages/index.ts` — `missing_role_branch_content` is now a 4th delegated defect class (`handleMissingRoleBranchContent()`), self-heals via `regenerate-child-content` instead of alert-only escalation; also added to the 5-minute held-only sweep's class filter via `cron.alter_job` on job id 9 (Addendum 36)
- `supabase/functions/regenerate-child-content/index.ts` — `DefectHint` type gained `"missing_role_branch_content"` (labeling only; `autoExpand`/`effectiveFieldsFor` deliberately don't branch on it) (Addendum 36)
- This ADR — the checklist itself, and the home for future addenda when a sweep finds and fixes something

## Addendum 39 (2026-09-10): worked through the Addendum 38 punch list live — one false alarm, one investigated-and-closed, one real bug found and fixed, one new permanent detector shipped

Jonathan asked to go through the 4-item punch list immediately rather than leave it as a future to-do. Each item got individually verified against current DB/code state before any action — per this project's own standing lesson (memory: "before recommending from memory... verify the memory is still correct") — which mattered: two of the four turned out to need correction from how Addendum 38 described them.

**1. `approved_concept_message_id` stale-snapshot gap (ADR-0069) — false alarm.** Addendum 38's punch-list description ("three candidate fixes drafted, never evaluated against each other") was stale — it reflected ADR-0069's original 2026-08-07 problem write-up, not its resolution. Reading the full ADR showed the gap was fixed 2026-08-11 (deterministic re-capture check, zero live cases found in a 46-conversation sweep at the time), a regression in that fix was found and fixed again 2026-09-05 (root-caused same day, 20 passing regression tests), and the one remaining piece (prose-only revision drift, no restated roster) is a reasoned, already-documented deferral, not an oversight — it's substantially covered by a separate fix (ADR-0059) and was never confirmed to cause real customer harm. Nothing built; the audit agent's summary was corrected, not acted on.

**2. `has_accomplice`-mismatch backlog, ~19-20 of 30 never individually checked (ADR-0120) — real bug found and fixed.** Full detail in ADR-0120 Addendum 2. Found one live, unfixed instance ("Operation: Thirty & Murdery" — ironically, the same package listed as already-remediated in ADR-0120 Addendum 1's own table, which had fixed a *different* leaked bracket in the same package and missed this second one). Fixed directly. The other ~24 confirmed clean. Also surfaced a separate, older, structurally different anomaly (7 packages with empty `detective_script`/`game_overview`/`host_guide` despite `generation_status='completed'`, dated 2025-08 to 2025-11, predating this project's active detector infrastructure) — flagged as a new open item, not folded into this scope.

**3. 2026-08-25 truncation near-miss, "worth a follow-up if a customer's roster comes back incomplete" — investigated, closed clean.** This was the same conversation as item 2's fix (Jaclyn, "Operation: Thirty & Murdery," 30-player order) — her concept message was truncated at exactly 8162 characters, cut off mid-sentence on character #30. Checked directly against her delivered `mystery_characters`: all 30 rows present, every name complete and well-formed (full `Title Name1/Name2 Surname` pattern, no truncation artifacts). The cutoff landed at the natural end of a complete 30-item list rather than mid-roster, so it never actually produced a Terminus-13-shaped broken character. Closed as investigated, no harm found — no action needed.

**4. Leaked German `Ihre/Deine` pronoun placeholder (Addendum 13) — new permanent detector shipped.** The one known historical instance ("Tod Auf Der Alm 3000," Glitch's section headers) was already hand-fixed at the time. A corpus-wide check found zero current live instances. Built anyway, per the new framework (cheap detection, and it's essentially free to extend an existing, already-proven detector rather than build a new one): added a new alternative to `list_packages_with_meta_text_leak()`'s marker regex — a word-bounded "formal-pronoun/informal-pronoun" adjacency check covering all 6 T-V-distinction languages this product supports (German, French, Spanish, Portuguese, Italian, Dutch), narrow enough to only match the literal leaked-placeholder shape (e.g. `Ihre/Deine` glued together with a slash), not the words appearing separately in normal prose. Verified zero false positives against the full corpus (character-level and package-level fields both checked independently) before and after deploying. Migration applied directly (`extend_meta_text_leak_with_unresolved_pronoun_pair`); no edge function redeploy needed since existing health-check calls (checks 6-9) pick up the updated function automatically.

**Net for this session: 2 real live bugs found and fixed (the Terminus 13 roster truncation this whole session started from, plus the second accomplice-bracket leak), 1 new detector shipped, 1 root cause fixed at the source, 2 items correctly closed as "investigated, no action needed" rather than either ignored or over-built.**

## Key files (Addendum 39)

- `public.list_packages_with_meta_text_leak()` (DB function) — extended marker regex, unresolved-pronoun-pair pattern
- Remediated directly (see ADR-0120 Addendum 2): `mystery_packages.detective_script` for "Operation: Thirty & Murdery"
- `/Users/jonathanmiller/Obsidian Vault/00_INBOX/deferred-bug-audit-2026-09-10-mystery-maker.md` — updated with resolution status for all 4 items, plus the newly-found null-`detective_script` anomaly

## Addendum 40 (2026-09-10): new bug class — pointform-only language regression, Haiku silently defaulting to English on a French package; root-caused, fixed at the source (moved `generate-pointform-summaries` off Haiku entirely), package repaired

New-purchase sweep on "La Voix Brisée Du Palais Garnier" (French, `script_type='pointForm'`, 7 characters, detective-style, non-murder "intrigue" mystery — the victim survives a blackmail letter, not a killing). All three standard detectors (`list_packages_with_meta_text_leak`, `list_packages_with_victim_mismatch`, `list_packages_with_unresolved_victim_name`) came back clean, and the manual victim-name cross-check and full `detective_script` read (no bracket leaks, register consistent) were clean too. `package_completion_blocking_defects()` also reported no blocking defects — this bug sits entirely outside what any existing detector or gate checks.

**What was found.** Every prose field (`background`, `relationships`, `secret`, `round2_script`/`round3_script`/`round4_script`, `accusations`, `final_statement`) was correctly in French for all 7 characters — the actual generation pipeline (Make.com Child, ADR-0093/0112-hardened) worked correctly. But the `*_pointform` summary fields — `introduction_pointform`, `rumors_pointform`, `round2/3/4_script_pointform`, `final_statement_pointform` — were in English for 6 of the 7 characters (all suspects), and correctly in French only for the murderer (Henriette Mercier). This is the same content, just re-expressed as bullets by a separate downstream summarization step, and since this package's `script_type` is `pointForm`, these are very likely what the host/players actually see.

**Root cause.** `supabase/functions/generate-pointform-summaries/index.ts` is the sole generator of every `_pointform` column, called once per character as the last step of the Make.com Child scenario (module 413 for detective-style, 525/521 for character-style). Its Anthropic call passes `packageId`/`characterName` only — no language parameter, ever, at any blueprint version checked (14 through 39). `SUMMARIZER_SYSTEM_PROMPT` had zero language instructions of any kind, relying entirely on the model implicitly mirroring the input language. This function was never touched by either prior language-hardening effort (ADR-0093, ADR-0112) — it fell outside both audits' scope because, by design, it takes no language parameter to audit. Confirmed via `git log -p --all` this has been true since the file's creation (2026-04-26); grepping its full history for "language" returns zero hits. This is a long-standing latent gap, not a recent regression — it had simply never been exercised/caught until this sweep, on this project's first live French `pointForm` package (confirmed via `profiles.language` join: of all paid, completed `script_type='pointForm'` packages, only 1 is tagged `fr` — this one).

**Fix, and why it needed a second pass.** First fix: added an explicit "write your bullets in the same language as the source text, do not translate to English" instruction to `SUMMARIZER_SYSTEM_PROMPT`, deployed (`generate-pointform-summaries` v9), and re-ran for the 6 broken characters. 4 of 6 came back correctly in French on the first try. The remaining 2 (Claudine Rocher, Marguerite Cassin) kept coming back in English across 3 more consecutive retries with the new instruction in place — not resolved by retrying, and not explained by anything unusual in their source content (read in full; ordinary well-formed French prose, no embedded English). Jonathan's call at that point: stop retrying on Haiku and move the function off it entirely. `MAIN_MODEL` changed from `claude-haiku-4-5-20251001` to `claude-sonnet-5` (matching `REVEAL_MODEL`, already Sonnet 5 for the unrelated confession-dropping reason documented above in this same file) — this is the same instruction-following reliability gap already known for `REVEAL_FIELDS` on Haiku (ADR-0074 lineage, ~93% drop rate on confession content), now shown to also affect `MAIN_FIELDS` language fidelity, at least for some characters. Deployed as v10, re-ran for the 2 remaining characters — clean in French on the first Sonnet 5 attempt. `summarizeCharacter()`'s two calls (`MAIN_FIELDS`, `REVEAL_FIELDS`) were left unmerged despite now sharing a model — each has its own prompt shape and failure history worth debugging independently if either regresses again; merging was judged unwarranted simplification for the cost/risk.

**Verification.** All 7 characters' `*_pointform` fields re-checked: zero English-word-signature hits (`\bthe\b|\bwas\b|\byou\b|\bher\b|\bhis\b`) and zero bracket-leak hits across `introduction_pointform`/`rumors_pointform`/`round2-4_script_pointform`/`final_statement_pointform`. `package_completion_blocking_defects()` re-run clean. No corruption of the (already-correct) prose fields — the summarizer is additive-only by design (only writes `_pointform` keys for fields it was asked to summarize).

**Cost note.** This function runs once per character on every `pointForm`-style package generation and repair, not just non-English ones — moving `MAIN_MODEL` to Sonnet 5 is a standing per-package cost increase across the whole product, not scoped to French packages. Jonathan made this call directly and explicitly ("avoid Haiku, use Sonnet 5 moving fwd") rather than it being scoped narrowly to the French-language failure mode; no further sign-off needed since the model itself, not incremental spend approval, was the decision.

**Not done:** no sweep of the 14 `profiles.language IS NULL` paid `pointForm` packages to check if any of them are actually non-English with the same unstated-language bug (the `fr`-tagged count check only found the 1 package fixed here) — `profiles.language` is a weak proxy since it can be unset even for English users; a real check would mean reading full package content per package, judged disproportionate to this sweep's scope. Flagged here rather than silently assumed clean.

## Key files (Addendum 40)

- `supabase/functions/generate-pointform-summaries/index.ts` — `SUMMARIZER_SYSTEM_PROMPT` gained an explicit same-language-as-source instruction; `MAIN_MODEL` changed from `claude-haiku-4-5-20251001` to `claude-sonnet-5` (now matches `REVEAL_MODEL`). Deployed v9 (prompt fix) then v10 (model change).
- Remediated directly: all 7 characters' `*_pointform` fields for `mystery_packages.id = 'be923b77-6d38-41ef-a2e2-411e14c132f1'` ("La Voix Brisée Du Palais Garnier")

## Addendum 41 (2026-09-10): Jonathan asked the obvious follow-up questions Addendum 40 left open — why did it happen, how do we stop it recurring, and why should a customer ever see it before it's fixed. Built a permanent detector, wired it into the completion gate and the self-heal pipeline, and found 3 more live broken packages (26 more characters) testing it

Addendum 40 fixed one package and the root cause, but left three things unanswered that Jonathan asked directly: (1) why 6 of 7 characters specifically drifted, (2) how this gets caught automatically instead of depending on a manual sweep, and (3) whether a customer could see a broken package before a fix lands — he explicitly wanted the package HELD longer, not delivered-then-silently-self-healed.

**Root cause, more precisely stated.** Not a deterministic murderer-vs-suspect branch — `generate-pointform-summaries` has no role-conditional logic anywhere in it or in the Make.com blueprints that call it. It's per-call stochastic drift: each character's pointform summary is one independent Sonnet/Haiku call with no explicit language anchor in the (100%-English) system prompt, so the model has nothing to override its default English bias except the source text's own language — which sometimes isn't enough. Confirmed empirically during the day's remediation work: even AFTER the Sonnet 5 switch, the same 2 characters (Claudine Rocher, Marguerite Cassin) needed a second retry before landing in French — the fix makes drift much rarer, not impossible. This is why detection had to be structural (compare pointform to the character's own prose) rather than trusting the generation step to self-certify.

**The detector.** Added a new blocking-defect check directly inside `package_completion_blocking_defects()` (the same function every other structural defect class already lives in — `missing_round_content`, `victim_mismatch`, etc.) plus a matching `list_packages_with_pointform_language_mismatch()` RPC for the self-heal delegation path, mirroring `list_packages_with_missing_role_branch_content()`'s shape exactly. Per character, it aggregates every prose field (`introduction`...`reveal_confession_accomplice`) into one blob and every `*_pointform` counterpart into another, then compares English-stopword density (`the|and|was|you|her|his|with|that|for|are|have|this`, word-bounded) between them: flag when the pointform blob reads clearly English (density > 3/1000 chars, ≥3 raw hits, length > 100 chars) while the prose blob reads clearly non-English (density < 1/1000 chars, length > 200 chars). Deliberately comparing the two fields to each other rather than to a stored "expected language" column — this needs no language field anywhere, works identically for all 13 supported languages including ones with no stored language value, and an English customer's prose is equally dense in stopwords so it structurally cannot false-positive. Tuned and verified against the full live corpus (~1200 characters across every package `generation_status='completed'`) before shipping: zero false positives at these thresholds, both before and after fixing the known instances.

**Testing the detector found 3 more live, previously-unknown broken packages.** Running the exact same comparison across the whole corpus (not just the French package from Addendum 40) surfaced:
- **"Tod Auf Der Alm 3000"** (German, 4 characters, all 4 flagged) — paid 2026-09-01, `ready_email_sent_at` already set 10 minutes after completion, meaning a customer had already received a "your mystery is ready" email for a package with English pointform content on every character. This is the same package flagged once before for an unrelated German register bug (ADR-0103 Addendum 13) — a second, independent defect on the same title.
- **"Veneno En La Medianoche"** (Spanish, 11 characters, 10 flagged) — paid 2026-08-22.
- **"El Último Brindis De Bellanotte"** (Spanish, 13 characters, 12 flagged) — paid 2026-08-22.

Neither Spanish package was tagged `language='fr'`/`'es'` on `profiles` — both were found under `language IS NULL`/`'en'`, which is why the earlier "only 1 fr-tagged pointForm package exists" corpus check in Addendum 40 undercounted the real blast radius. This is exactly why the detector compares prose-to-pointform directly instead of trusting `profiles.language`: it caught both Spanish packages with zero dependency on that unreliable column. Total across all 4 packages found to date: 33 characters, 4 paying customers, at least 1 of whom had already been notified their (defective) mystery was ready.

**Fixing the 3 newly-found packages surfaced a second, distinct bug: `max_tokens: 4000` was too low for long characters.** Three characters in "Veneno En La Medianoche" (30-34K characters of source prose each — character-style role-variant fields, the longest in the corpus) hit the ceiling mid-JSON-string on the first regeneration attempt, producing unparseable truncated output (`Unterminated string in JSON`). Same shape as the Terminus 13 incident elsewhere in this ADR (Addendum 37/38) — a token cap nobody had deliberately tuned for this prompt. Raised `max_tokens` 4000 → 8000 (deployed as v11; costs nothing unless a reply actually needs the tokens) and retried — all 3 succeeded. Also discovered mid-repair that `generate-pointform-summaries` processes characters serially within one request and the Supabase gateway enforces a 150-second idle timeout: a bulk call with 10-12 `characterIds` silently times out gateway-side after processing roughly the first 6-9, leaving the rest untouched with no error surfaced to the caller (the HTTP request itself just dies). Discovered by checking `updated_at` timestamps after an apparently-failed call and finding partial progress. Both engineering findings fed directly into the self-heal handler design below (batch size, not bulk).

**All 4 packages re-verified clean:** corpus-wide re-scan after all fixes returned zero flagged characters anywhere, `package_completion_blocking_defects()` returns `NULL` for all 4 (was already true for 3 of them before this addendum's new check even existed; confirmed the new check doesn't regress that).

**Completion gate — directly answers "hold the mystery longer."** Because the new check lives inside `package_completion_blocking_defects()`, it's automatically covered by the existing `validate_package_characters()` trigger (unchanged — no new trigger needed): any package whose generation transitions toward `'completed'` with a pointform language mismatch present gets its status rewritten to `'needs_review'` instead, before that row write is even visible to any other trigger. Traced the full downstream consequence: `trg_z_notify_package_ready` (the only path that ever calls `send-mystery-ready-email`, per the 2026-08-23 "single source of truth" migration) is a same-table `BEFORE` trigger deliberately named to fire *after* `trg_validate_package_characters` (alphabetical ordering, fixed for exactly this race in the 2026-08-31 incident referenced in that migration) and gates strictly on `generation_status->>'status' = 'completed'`. So a package caught by this new check never reaches `'completed'` in the row `trg_z_notify_package_ready` reads, and the customer-facing ready email structurally cannot fire for it. No new gating code was needed — this was the payoff of reusing the existing blocking-defect function rather than building a parallel check.

**Self-heal — directly answers "without me having to do a sweep."** Added `pointform_language_mismatch` as an 8th defect class to `auto-remediate-packages`. This class needed a different shape than any existing one: `list_packages_with_missing_role_branch_content` (the closest precedent) returns one row per flagged character and each row triggers its own `runGatedAttempt`/`delegateToRegenerator` call — fine for that class, since role-branch gaps are rare and usually isolated to 1-2 characters. Language drift is the opposite pattern: this incident alone showed 4-12 characters flagged per package at once, and the 2-attempt cap in `runGatedAttempt` is a *per-package-per-defect-class* budget, not per-character — calling it once per character would burn the whole cap on the first 2 of 12 and escalate the other 10 untouched, never actually clearing the block. Fixed by grouping detector rows by `package_id` in `serve()` before dispatch, so `handlePointformLanguageMismatch` receives the full list of a package's flagged characters in one call → one `runGatedAttempt` → one attempt-cap slot covering everyone. Internally it still calls `generate-pointform-summaries` in batches of 4 (`POINTFORM_BATCH_SIZE`, chosen from the 150s-gateway-timeout finding above — 4 characters stays comfortably under that ceiling even at Sonnet 5's slower per-character pace) rather than one bulk request. `revert` is `async () => false` (same rationale as `handleMissingImages`'s an-extra-image-is-never-worse-than-a-missing-one) — there's no single "before" text worth restoring since the prior pointform content was already wrong, and the re-detect gate inside `runGatedAttempt` still catches and escalates a fix that didn't fully take.

Wired into both cron lanes: the existing 30-minute full sweep (`auto-remediate-packages` job, posts `{}`, no class filter — picks up the new class automatically with no config change) and, matching the ADR-0103 Addendum 36 precedent for `missing_role_branch_content`, added explicitly to the 5-minute **held-only** sweep's class filter (`cron.alter_job` on job id 9) so a newly-generated package held by this gate self-heals within ~5 minutes rather than waiting up to 30. Verified via three live `dry_run` calls against the actual deployed function post-deploy (unfiltered, class-filtered, and the exact payload the 5-minute cron now sends) — all executed cleanly with zero errors; `considered: 0` for the new class specifically since nothing is currently flagged, and 3 unrelated pre-existing `missing_role_branch_content` rows surfaced correctly on the combined-class dry run, confirming the new class didn't break dispatch for the others.

**What "hold longer" now actually means end-to-end for a future occurrence:** generation completes → `validate_package_characters()` catches the mismatch → status held at `needs_review`, ready-email structurally blocked, `notify-generation-issue` fires (internal alert, not customer-facing) → within 5 minutes the held-only sweep detects and repairs it via Sonnet 5 → re-detect gate confirms clean → next write to `generation_status` (nothing else needs to touch it; the package was never `'completed'` to begin with, so the *first* legitimate completion is the post-fix one) reaches `'completed'` for the first time → `trg_z_notify_package_ready` fires then, on the corrected content. A customer never sees a `'completed'` status carrying this defect, and in the common case never experiences a delay longer than the 5-minute sweep cadence.

**Not done:** did not sweep the remaining packages under `profiles.language IS NULL`/`'en'` beyond what the full corpus scan already covered (the scan checked every `completed` package's actual content regardless of the `language` column, so this is already fully covered — noting explicitly so a future reader doesn't re-flag it as a gap). Did not build a customer-facing "your mystery needed a quick fix" notification for the held-then-self-healed case — `ready_email_sent_at` simply lands later than usual, silently, matching how every other self-healed `needs_review` class already behaves in this product.

## Key files (Addendum 41)

- `package_completion_blocking_defects()` (DB function) — new `pointform_language_mismatch` defect class, per-character prose-vs-pointform stopword-density comparison, mystery-style-agnostic
- `list_packages_with_pointform_language_mismatch()` (DB function, new) — the `list_packages_with_*` counterpart for `auto-remediate-packages`'s delegation loop, same shape as `list_packages_with_missing_role_branch_content()`
- `supabase/functions/generate-pointform-summaries/index.ts` — `max_tokens` raised 4000 → 8000 (deployed v11)
- `supabase/functions/auto-remediate-packages/index.ts` — `pointform_language_mismatch` is now an 8th defect class (`handlePointformLanguageMismatch()`), grouped-by-package dispatch (new pattern, distinct from the existing one-row-one-call classes), batches calls to `generate-pointform-summaries` at `POINTFORM_BATCH_SIZE = 4` to stay under the gateway's 150s idle timeout; wired into both the 30-minute full sweep (automatic) and the 5-minute held-only sweep (`cron.alter_job` on job id 9, explicit class-list addition)
- Remediated directly: all characters' `*_pointform` fields for `mystery_packages.id` = `1773bfb5-93a3-4313-865e-4b40f35d9a13` ("Tod Auf Der Alm 3000"), `7c56a42d-a7ca-4a84-b7a0-0f507f32ea83` ("Veneno En La Medianoche"), `c28e31f6-f821-4dc6-abc3-c555a07ad9bb` ("El Último Brindis De Bellanotte")

## Addendum 42 (2026-09-11): new bug class — wrong grammatical person and self-referential name leaks in the accomplice branch, found on a 3-package sweep; two clean, one fixed

Routine 3-purchase sweep. **The Seeker's Last Stand** (detective-style, 5 chars) and **Badge Of Dishonor** (detective-style, 16 chars) were both clean end to end — all three detectors, `package_completion_blocking_defects()`, full detective-script read, and 3-5 full character records each turned up nothing.

**The Multiverse Gala: Who Silenced The Rebel?** (`mystery_style='character'`, no fixed culprit, `has_accomplice=true`, 13 characters, package_id `cb383fb3-d8ab-4a4f-a908-77114d2097b0`) passed the peer-existence check (all 13 had every accomplice-branch field populated — the ADR-0096 gap wasn't present) but a manual read of the actual branch content surfaced a defect shape the checklist doesn't cover: **branch scripts written in the wrong grammatical person, and two characters' own confessions self-referencing their own name in the third person.**

Specifics found:
- **Arthur** — `round2/3/4_guilty` written in second person ("You don't bother denying it...") instead of first person like every other character's guilty branch; all 5 of his `*_accomplice` fields (round2, round3, round4, final, reveal_confession) written in third-person narration ("Arthur spreads his hands... he says...") instead of first-person dialogue.
- **Chamber/Chantal** — same third-person-narration bug across all 5 of his `*_accomplice` fields; his guilty/innocent branches were correctly first person.
- **Korra** — `reveal_confession_accomplice` and its pointform twin referred to "Korra" in the third person while nominally speaking as Korra — i.e., her own confession described her protecting herself.
- **Sakuta/Sakura Azusagawa** — same self-reference bug in `reveal_confession_accomplice`, additionally borrowing Hiroko's "astral arms" ability (which isn't his) and naming himself by full name mid-confession. All 5 of his `*_accomplice_pointform` fields were also simply `NULL` (a second, distinct completeness gap — full text existed, pointform summaries were never generated) — this is the uniform-null shape ADR-0113's trigger can't see, same class as the Addendum-8/ADR-0113 pointform-null find, just isolated to the accomplice branch of one character. **Astra**'s `reveal_confession_accomplice_pointform` was also `NULL` for the same reason.

In a live game, if the host randomly assigns any of these roles to the affected characters, the player is handed a script written *about* them instead of *by* them — unreadable as dialogue, and for Sakuta specifically, the host has no pointform quick-reference for that branch at all.

**Root cause not investigated** — likely the generation prompt for the accomplice/guilty branches doesn't pin person consistently the way the innocent branch apparently does (11 of 13 characters' guilty/innocent/accomplice branches were correctly first person; only these specific fields drifted), but this wasn't traced further given the fix was a direct content rewrite, not a pipeline change.

**Fixed directly** (Jonathan confirmed before the rewrite, given the volume of narrative text touched): rewrote all 9 affected Arthur fields and 5 affected Chamber fields to first person preserving every plot fact, motive, and evidence reference; rewrote Korra's and Sakuta's `reveal_confession_accomplice` (and Korra's pointform twin) to remove the self-reference, keeping them generic/unnamed about who they protected (matching the existing pattern used by several other characters' vague accomplice confessions, e.g. Jett/Jae's); wrote the 5 missing Sakuta pointform fields and Astra's 1 missing pointform field from the existing (correct) full-text content. Re-ran `package_completion_blocking_defects()` and `list_packages_with_meta_text_leak()` post-fix — both clean.

**Not done:** no detector built for this bug class — first occurrence, matches this ADR's own established bar (2+ live instances before shipping a detector, see Addendum 31) and a "does this read in first person" check isn't obviously cheap to build reliably (would need to distinguish a character's own rhetorical "you"/"they" from an actual person-consistency break, similar to the semantic-contradiction problem noted in `CLAUDE.md`'s secret-inversion check). If this recurs on a future sweep, that's the trigger to revisit with Jonathan. No sweep of other completed `has_accomplice=true` / `mystery_style='character'` packages for the same defect — only this one package was in scope for this sweep.

### Key files (Addendum 42)
- Direct data fixes only, no code changes: `mystery_characters` rows for `character_name` in `('Arthur','Chamber/Chantal','Korra','Sakuta/Sakura Azusagawa','Astra')`, `package_id = 'cb383fb3-d8ab-4a4f-a908-77114d2097b0'`

## Addendum 43 (2026-09-11): Terminus 13's roster completed from 15 to 28 characters — the customer confirmed she wants the full cast, closing the Addendum 37 scope question

Addendum 37 (2026-09-10) fixed Terminus 13's one broken character name but deliberately left open whether to complete the roster to the 28 characters the customer designed, since `player_count`/the delivered package only covered 15 and completing it meant real Anthropic spend needing explicit sign-off. The customer (Angie Smedley) replied confirming she wants the full 28. Jonathan authorized completing it.

**No existing tool does this.** `regenerate-child-content` repairs fields on existing character rows; it cannot create new ones. `adapt-mystery-apply` (the paid "Remove a Character" pipeline) only removes. Building a one-off completion required a new script.

**Approach:** rather than inventing new prompts, copied the actual production prompt-building logic verbatim from `regenerate-child-content/index.ts` (`roleBlock`, `contentCoherenceRules`, the `GROUP_IDENTITY`/`GROUP_SOCIAL_DETECTIVE`/`GROUP_ROUNDS_DETECTIVE` schemas, `buildPrompt`, `parseJsonResponse`) into a standalone script (`scripts/complete-terminus13-roster.mjs`, gitignored — one-off, not a standing tool) — not imported via the usual esbuild-transpile technique, since that file's top-level Deno/Supabase-client setup isn't Node-importable, and not run through the deployed edge function directly, since its shared `$5/day` remediation spend cap (already at $1.23 spent elsewhere today) couldn't cover this job's ~$4 cost without either bumping a shared production constant or running across a day boundary — neither acceptable for a customer waiting on a same-day fix. A verbatim copy for a single-use task script, not a permanent parallel implementation, was judged an acceptable one-time exception to the "never a hand-copy" (ADR-0057) discipline that governs standing detectors.

**Confirmed clean-game_overview/detective_script/evidence_cards/materials before writing anything:** none reference the character count numerically (game_overview says "nearly everyone... had reason," evidence cards say "at least nine residents," detective_script names the culprits by name, not by roster size) — so growing the cast from 15 to 28 needed no changes to any of those fields. `master_context` (the internal JSON planning document other tools read for coherence) was deliberately NOT updated with the new 13 characters — it's 76KB, not valid single JSON (a malformed concatenation, confirmed live), and its exact consumption pattern by other tools wasn't fully mapped; flagged as a known limitation for any future repair tool invoked against this package, not fixed here.

**Named 13 new suspects** across the concept's original faction plan (Civilians ×5, Remembered ×6, "Unknown Operatives" ×2 — the last renamed to ordinary outsider/fringe suspects since the original plan's "one Unknown Operative is the accomplice" design was already superseded by what actually generated: the accomplice role went to Weapons Master Kitrina Boone, from Security, and that's already locked into the delivered detective_script's reveal). Deliberately did NOT resurrect the customer's original "someone has been secretly opening the tunnel doors" second-conspiracy thread — that subplot never made it into the actual generated `game_overview`/`detective_script` at all (confirmed by reading both in full), so reintroducing it now via the new characters would create a NEW inconsistency (new characters referencing a plot thread nothing else in the delivered game supports) rather than resolve one. All 13 assigned `character_role = 'suspect'` — no new red herrings, since master_context's `misdirectionTargets` already names exactly 3 established red herrings and adding more without backing master_context data risks an unbacked mechanical distinction.

**Validated on one character before committing to the full batch:** ran the pipeline for character #1 alone, read the full output — correctly avoided implicating the character in the actual murder, correctly cross-referenced both existing (old 15) and not-yet-generated (new 13, by name only at that point) cast members, no tunnel-door subplot leak, voice/quality matched the existing 15. Proceeded to the remaining 12 only after this check.

**Hit two live issues mid-batch, both are the same known recurring JSON-escaping problem `regenerate-child-content`'s own code comments already document** (apostrophe/control-character escaping, occasional leaked `<think>` text preceding the JSON payload eating into the token budget): one hard crash after 3 consecutive parse failures on one character (root cause: `MAX_TOKENS` was 8000, undersized for this prompt's richest group under an occasional thinking-leak tax), fixed by raising to 12000 and adding a bounded 5-attempt retry loop; the script's idempotent resume logic (skip any character/group whose fields are already populated) meant no completed work was lost or redone across the two restarts.

**Verified, not just trusted the script's exit code.** 28 total characters (was 15), exactly 1 murderer + 1 accomplice (unchanged — the pre-existing two, not touched), zero rows with any null field. Re-ran the full detector suite used throughout this ADR (`meta_text_leak`, `victim_mismatch`, `unresolved_victim_name`, `evidence_culprit_spoiler`, `final_statement_confession_leak`, `identity_conflicts`, `missing_role_branch_content`, `self_directed_questions`, `slip_culprit_leak`, `unconfessed_culprit`, `victim_as_character`, `structural_defects`, `package_completion_blocking_defects()`) — all clean. Grepped every new character's every field for leaked `<think>`/`</think>` tags, raw unescaped JSON fragments, and chain-of-thought phrases — zero hits. Manually read 4 of the 13 in full (including both characters that needed JSON retries, to confirm the eventually-successful version was genuinely good, not just technically parseable) — all high quality, consistent voice, correctly avoid spoiling the murderer's identity. One new character's rumor mentions the maintenance-corridor sighting attributed to Registry Guard Beckett Sorrow — checked against `master_context`: Sorrow is one of the murderer's 3 already-established `misdirectionTargets`, so this is legitimate designed misdirection, not a leak.

**Total cost across validation + two resumed batches: approximately $3.75-4.00** (usage-based actual pricing, not the flat per-call estimate `regenerate-child-content` uses — came in lower than the initial ~$3.90 flat-rate estimate given to Jonathan before starting).

**Not done:** `master_context` not extended with the new 13 (see above); `extracted_characters` not extended either — confirmed live it's stored as a malformed non-JSON concatenation predating this fix, so the script passes each character's seed description directly as a parameter instead of depending on that field, and left it untouched rather than risk rewriting an already-broken field whose consumers aren't fully mapped; the still-open `has_accomplice`-flag-vs-content backfill question from ADR-0120 is unrelated and untouched.

### Key files (Addendum 43)
- `scripts/complete-terminus13-roster.mjs` — one-off completion script, not committed (gitignored, `*.mjs` blanket rule, deliberately not added to the allowlist since this is single-use)
- `mystery_characters` — 13 new rows for `package_id = 'e86e0bee-560e-435b-910a-5e636e0421bc'`
